"""tdx 数据延长脚手架（2026-08-17 记录，待执行）。

目标：把 au/ag/m 主力连续序列延长到 2026-08（当前基准数据截止 2024-07-17）。

现状（2026-08-17 实测）：
- sina 源：数据亦截止 2024-07-17（与基准同源），无法延长；
- 通达信 tdx MCP：可拉具体合约日线（target=1），如 AU2612 覆盖 2025-11~2026-08，
  但缺 2024-07~2025-11 段（需 AU2412/AU2512 等合约拼接）；
- westock MCP：期货仅支持现货（伦敦金/黄金9999），不支持主力连续。

待执行方案（需 MCP 交互，非本脚本自动完成）：
1. 对每个品种拉 2024-07 至今的全部主力合约日线（约 5-8 合约/品种）：
   - AU: AU2412/AU2506/AU2512/AU2606/AU2612/AU2702...
   - AG: 同 AU 月份结构；M: M2501/M2509/M2601/M2609...
2. 主力判定：每交易日取 VolInStock（持仓量）最大合约；
3. 连续拼接：换月日按比例调整（新合约×ratio 对齐旧合约末值），生成 au0/ag0/m0 延长序列；
4. 落盘 data/raw/processed/{sym}/1d/{year}.parquet（与现有分片格式一致）；
5. 重跑 SENTINEL 全链路（OOS 2024-07~2026-08，真正的新数据验证）。

注意：
- tdx 期货 K 线需显式 target="1"，code=品种缩写+合约月份（如 AU2612），setcode=30(上期所)/29(大商所)；
- 每次调用返回 ~182 根×14 字段的冗长 JSON，建议按品种逐合约处理并落盘 CSV 中间件，勿全量留在会话上下文；
- 拼接后需与基准尾部（2024-07-17）做连续性校验（比例误差 <0.1%）。
"""


# ============================================================================
# 实测结论（2026-08-17 执行后更新）——【数据延长在此环境不可行】
# ============================================================================
# 1. tdx_kline 仅返回【未交割/当前活跃】合约：
#    - AU2612（当前主力）: 182 根 2025-11-18~2026-08-17 ✅ 可拉
#    - AU2412 / AU2508（已交割）: Rows=[] ❌ 无历史
# 2. 缺口分析（2024-07~2026-08 目标区间）：
#    - 2024-07 ~ 2025-11: 主力合约（AU2412/AU2506/AU2512 等）均已交割 → tdx 无数据；
#      sina 本地源同截止 2024-07；westock 期货仅现货 → 【不可补】
#    - 2025-11 ~ 2026-08: 当前主力单合约可拉，但无 120 日 warm-up 历史（特征归一化
#      需要 normalize_window=120 前置数据），且与基准序列不连续 → 无法直接跑 SENTINEL
# 3. 结论：SENTINEL 最终 OOS 验证只能用现有数据（重排分段：OOS 2023-05~2024-07，
#    240 信号，RL Sharpe 3.83 vs 规则基线 0.88）。真新数据验证需：
#    - 本地 pytdx 网络恢复（公共行情服务器当前不可达）；或
#    - 接入其他含历史主力连续的数据服务商。
# ============================================================================
