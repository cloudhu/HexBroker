# HexFutures-AI 依赖清单（Windows 友好版，已对齐 Kronos 精确 pin）
# 安装：C:/Users/Administrator/.workbuddy/binaries/python/envs/default/Scripts/pip.exe install -r requirements.txt
# 注：torch / stable-baselines3 为可选重依赖（CPU 沙箱可省略，系统会自动回退纯 numpy 实现）。
#
# 单一事实源：核心运行依赖以 pyproject.toml [project].dependencies 为准（共 15 项）
# （新人推荐 `pip install -e .[dev]`，可选重依赖用 `pip install -e .[torch]` / `.[sb3]`）。
# 下方 15 项核心依赖仅为向后兼容保留（与 pyproject 同步维护）；pytest 不在核心 15 项中，
# 对应 pyproject 的 [dev] extra，此处保留 pytest 行仅供 `pip install -r requirements.txt` 兼容。
# 以后新增/修改依赖请优先改 pyproject.toml，再回填本文件保持一致。

numpy>=1.26,<3
pandas>=2.2.3,<4
scipy>=1.11
scikit-learn>=1.3
joblib>=1.3
pyyaml>=6.0
omegaconf>=2.3
pydantic>=2.6
optuna>=3.6
pyarrow>=14.0
matplotlib>=3.9
loguru>=0.7
rich>=13
tqdm>=4.67
lightgbm>=4.5,<5
pytest>=8  # dev-only：pyproject 中对应 [dev] extra，此处保留供 -r 兼容

# ---------- 可选（heavy，不阻塞离线链路） ----------
# gymnasium 为 stable-baselines3 传递依赖（RL 环境为自研 futures_env，不直接引用），已移入 [sb3] extra
# torch>=2.1,<3   # 上限 <3：env 实测 torch 2.11 跑 Kronos 微调正常，旧 <2.6 上限过时
# stable-baselines3>=2.3,<2.7
# sb3-contrib>=2.3

