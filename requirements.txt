# HexFutures-AI 核心依赖（Windows 友好版，已对齐 Kronos 精确 pin）
# 安装：C:/Users/Administrator/.workbuddy/binaries/python/envs/default/Scripts/pip.exe install -r requirements.txt
# 注：torch / stable-baselines3 为可选重依赖（CPU 沙箱可省略，系统会自动回退纯 numpy 实现）。

numpy>=1.24
pandas==2.2.2
scipy>=1.11
scikit-learn>=1.3
joblib>=1.3
pyyaml>=6.0
omegaconf>=2.3
pydantic>=2.6
optuna>=3.6
pyarrow>=14.0
matplotlib>=3.9
loguru>=0.7
rich>=13
tqdm>=4.67
gymnasium>=0.29,<1.1
lightgbm>=4.1
pytest>=8

# ---------- 可选（heavy，不阻塞离线链路） ----------
# torch>=2.1,<2.6
# stable-baselines3>=2.3,<2.7
# sb3-contrib>=2.3
