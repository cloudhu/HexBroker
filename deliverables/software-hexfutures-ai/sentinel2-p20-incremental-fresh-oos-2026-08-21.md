# Sentinel-2 P20 交付报告：增量数据更新 + S4 fresh-OOS 复核 + 生产流水线首次运行

- **日期**：2026-08-21
- **执行人**：software-engineer (Kou)
- **项目**：HexBroker（E:/Workspace/HexBroker）
- **上游**：P6-P19 全部 QA VERIFIED；P19 生产配置 A30/B70 + nf_b 0.30 已固化（commit fed0912）

---

## 1. P20-1 增量数据更新（18 品种 08-18 ~ 08-20）

### 1.1 拉取
- 数据源：PandaData `get_future_daily_post`（method=**close_pcr** 后复权主力连续）
- 窗口：20260818 ~ 20260820；18 品种一次性拉取，网关返回 **54 行 = 18 品种 × 3 交易日**（truncated=false）
- 持久化：`artifacts/p6_4_raw/seg_20260820_ALL.json`（小响应手动落盘，格式与 p6_4 `load_persisted_rows` 兼容）

### 1.2 合并写回（复用 p6_4_fill_gaps.py --stage parse，seg=20260818_20260820）
- 18 品种逐一 parse 合并：`data/raw/processed/{sym0}/1d/2026.parquet` **147 → 150 行（+3）**
- 写前备份：`artifacts/backup_p64/{sym0}_2026_prefill_20260821_*.parquet`（18 份）
- 幂等登记：`artifacts/p6_4_applied.json` 新增 18 条（seg=20260818_20260820），overlap 偏差全 0

### 1.3 口径换算确认（RAW_SCALE_FIX，仅 au0/ag0/m0）
| 品种 | close_pcr 源（08-20） | 系数 | 落盘 close | 逐位一致 |
|---|---|---|---|---|
| au0 | 746.282402 | 1.2596349591656173 | 940.043403 | ✅ |
| ag0 | 11189.453502 | 1.4505499241026856 | 16230.860928 | ✅ |
| m0 | 10283.221030 | 0.31682966433418885 | 3258.029467 | ✅ |

其余 15 品种本地为 close_pcr 后复权口径，系数=1.0 直接可用。

### 1.4 数据完整性检查
- **18 品种最新日期全部 = 2026-08-20** ✅（无停牌/缺数据品种）
- 边界连续性 08-17→08-18 close 日变动均正常（最大 sc0 +4.13%，原油波动属正常，无 >15% 伪跳变）
- `p6_4_fill_gaps.py --stage verify`：**PASS**（剩余窗口内缺口 0d，重叠偏差 0 处）
- 说明：08-21 当日数据未拉取（按任务要求拉 08-18~08-20）；基差/现货面板（data/raw/fundamental）未更新（止 08-17，sc0 止 04-30，不在本次范围）

---

## 2. P20-2 S4 影子监控 fresh-OOS 复核

运行：`python scripts/p12_s4_shadow_monitor.py --monitor`（基线 = P12 快照，kline_max_date=2026-08-17）

### 2.1 复核输出
| 指标（63 窗滚动） | 基线 | 复核 | 变化 |
|---|---|---|---|
| v2 滚动 IC | -0.313078 | -0.313078 | 无 |
| v2 滚动命中率 | 0.375397 | 0.375397 | 无 |
| rt30 滚动 IC | -0.025730 | -0.025730 | 无 |
| rt30 滚动命中率 | 0.527778 | 0.527778 | 无 |
| 池化信号相关性 | 0.603704 | 0.603704 | 无 |
| fresh_kline | — | **YES**（08-20 > 08-17） | 数据新鲜度翻转为是 |
| fresh_signal | — | NO（信号缓存未变） | — |
| **主触发** | — | **UPGRADE_TRIGGER**（IC 连续胜 7/5，命中率连续胜 3/5，fresh 确认 YES） | 形式上触发 |
| 漂移 | — | DRIFT_OK | — |

### 2.2 关键判断（如实报告）
- **触发条件形式上已满足**：新增 08-18~08-20 K 线后，P12 登记触发点（fresh-OOS 新数据）被确认，且 rt30 在共同尾部（OOS 内、对齐至 v2 末信号日 06-10）对 v2 取得 **连续 7 窗滚动 IC 优势**（> 阈值 5 窗）→ 判定 UPGRADE_TRIGGER。
- **但增量数据未提供任何新信号证据**：复核全部指标与基线**逐位相同**。原因：
  1. 信号缓存 v2/rt30 是 walk_forward 产出，分别止于 2026-06-11 / 2026-07-27，本次未重建；
  2. 所有既有信号日的 fwd5 收益在数据止于 08-17 时已可计算（最晚信号日 07-27 的 fwd5 需 08-03，已覆盖）——08-18~08-20 三天 K 线**没有**进入任何滚动 IC/命中率/相关性窗口。
- 因此本次 `fresh_confirmed=YES` 是**数据新鲜度书签式确认**，非信号绩效的 fresh-OOS 确认；rt30 的相对优势（IC -0.026 vs -0.313、命中率 0.528 vs 0.375）全部位于 P7 OOS 段内（回顾性），与基线时一致。

### 2.3 结论
**不触发升级评估（如实报告）**。rt30 / F2 候选**暂不升级**：
- 若要获得真正的 fresh-OOS 证据，需（a）以 08-18~08-20 数据重建信号缓存（walk_forward 重训产出新信号日），或（b）积累更多 fresh-OOS 交易日后再复核；
- 漂移 DRIFT_OK，两引擎未分化，无告警事项。

---

## 3. P20-3 生产流水线首次运行（A30/B70 新配置）

### 3.1 P16 交易计划（2026-08-20）
运行：`python scripts/p16_daily_signal.py --date 2026-08-20`
- 配置确认：引擎 A（v8 cache + top30% + S2 min=3 + cap 0.5）+ 引擎 B（win252/thr0.7 + nf_b **0.30**）+ 组合 **A30/B70**（vol_target=False，initial_capital 1,000,000）
- 数据完整性：K 线 18 品种最新 2026-08-20；基差最新 2026-08-17（sc0 04-30）→ WARN×2（sc0 基差过期/断层，历史已知）
- 引擎 A：**无信号**（fold 结构性截断：v8 缓存最后信号日 2026-06-29，目标日在其后——P6-3b 已登记）
- 引擎 B：**0 品种选中**（基差面板止于 08-17，08-20 无 br_rank 行）
- **08-20 交易计划 = 空计划**（0 手 / 0 名义 CNY / 0.0% 权益；风控 PASS）
- 落盘：`artifacts/trade_plans/2026-08-20_sentinel2_plan.json` + `.csv` ✅

> 空计划成因（如实）：引擎 A 缓存 fold 截断（结构性，与数据无关）+ 引擎 B 基差数据未随本次 K 线更新（基差更新不在 P20 范围）。流水线本身运行正常、无报错。

### 3.2 P17 模拟盘（shadow account）
运行：`python scripts/p17_shadow_account.py`（默认：ferrous=block / T+1 开盘 / 滑点 1 tick / 费 0.005%）
- **重要说明**：p17 **无 --apply/增量入账模式**，仅支持全量 OOS 回放（OOS_END 硬编码 2026-08-17，本次未改脚本——只读约束）。因此"首次运行方式"= 全量回放。
- **本次为 A30/B70 + nf_b0.30 终裁配置下的首次完整回放**。P17/P19 交付文档中的旧状态（期末 1,090,232 / BT 纯B 1,094,051）为**旧 A10/B90 + nf_b0.20 配置**产出，已被本次结果取代（两者不可直接比较；P19 曾生成 08-17 单日 A30/B70 计划，但未重跑全量回放）。
- 回放窗口：OOS 502 交易日（2024-07-18 ~ 2026-08-17）；K 线已延至 08-20，但窗口按 OOS_END 仍止于 08-17。

**模拟盘最新状态（2026-08-17 收盘）**
| 指标 | 值 |
|---|---|
| 期末权益 | **1,150,947.65 CNY（+15.09%）** |
| 年化收益 | 7.33% |
| 最大回撤 | -9.50% |
| Sharpe | 0.763 |
| 成交 | 1000 笔（买 995 / 卖 989 手） |
| 执行计划 | 451 | 黑色系 FAIL 阻断 50 | pending 0 |
| 纯 B 日 | 381（有持仓日 491） |
| 最终持仓（08-17） | al0 2手、ni0 1手、ta0 3手（均引擎 B，来自 08-14 计划 08-17 开盘成交） |
| 已实现/未实现 PnL | +147,312.64 / +3,635.01 |
| 一致性 | bt~sa_block 日收益相关 0.9624；bt~sa_samebar 0.9984；BT 纯 B OOS 期末 1,120,279（505 日） |

- 08-20 计划为空 → **无增量入账**（如实说明）。
- 已知记账口径现象：pending 计数由 1 → 0（因价格已延至 08-20，p17 的 pending 判定 `any(d>dates[-1])` 翻转）；但 08-17 计划实际仍未在回放窗口内执行（exec 日 08-18 超出窗口）。此为硬编码 OOS_END 的已知限制。

---

## 4. 落盘清单
- `artifacts/p20_data_update.log`（增量更新日志）
- `artifacts/p6_4_raw/seg_20260820_ALL.json`（PandaData 原始响应）
- `artifacts/backup_p64/*_2026_prefill_20260821_*.parquet`（18 份写前备份）
- `artifacts/p6_4_applied.json`（+18 条 applied 记录）
- `artifacts/p20_s4_monitor.log`（S4 复核日志 + JSON）
- `artifacts/trade_plans/2026-08-20_sentinel2_plan.json/.csv`（P20-4 后为**非空计划**：al0/ta0/zn0 共 6 手；初版为空，见 §7）
- `artifacts/p20_p16_20260820.log`、`artifacts/p20_p17_run.log`
- `artifacts/shadow_account/*`（equity_curve / positions_history / plan_executions / consistency_compare / p17_shadow_account.json / plans_cache 502 份）
- `deliverables/software-hexfutures-ai/sentinel2-p20-incremental-fresh-oos-2026-08-21.md`（本报告）

## 5. 交付结论
1. **增量更新**：18 品种最新日期全部 = 2026-08-20；au0/ag0/m0 RAW_SCALE_FIX 换算逐位确认；备份完整；p6_4 verify PASS。
2. **S4 fresh-OOS 复核**：形式上触发（UPGRADE_TRIGGER + fresh 确认），但增量数据未提供新信号证据（指标与基线逐位相同）→ **rt30/F2 不升级**，等待信号缓存重建或更多 fresh-OOS 日。
3. **生产流水线首次运行**：p16 输出 08-20 初版**空计划**（引擎 A fold 截断 + 引擎 B 基差未更新）；p17 首次以 A30/B70 终裁配置完成全量回放，模拟盘期末 **1,150,947.65（+15.09%）**。**P20-4 基差补齐后 08-20 计划转非空**（al0/ta0/zn0 共 6 手，见 §7）。

## 6. IS_PASS 判定
**IS_PASS: YES**（各任务如实完成；附带以下已如实标注的限制，不影响通过判定）：
- ~~08-20 交易计划为空~~ → **P20-4 已修复**：基差补齐后 08-20 计划转非空（al0/ta0/zn0 6 手）；SC 基差源端缺失（止 04-30）如实保留
- S4 触发为数据新鲜度书签式确认，不构成升级依据（不升级）
- p17 仅支持全量回放（无 --apply），且 OOS_END 硬编码 08-17；本次为 A30/B70 首个完整回放，取代 P17/P19 文档中的旧 A10/B90 状态
- D1 完整性检查已修复（严格口径 latest < target_date 即 WARN）+ 空计划语义标注（plan_note）已落地（见 §7）

---

## 7. P20-4 基差增量补齐 + D1 修复 + 空计划标注（QA 建议落实）

### 7.1 基差增量补齐（17 品种 → 08-20）
- 探测确认：PandaData `get_future_basis`（DeepView 基差接口）网关参数名为 **symbol**（`underlying_symbol` 报 unknown params，与 fetch_deepview_data.py 记录一致）；响应 result[] 数组含 basis / basis_ratio / date / spot_price / underlying_symbol
- 拉取 18 品种 08-18~08-20 → **17 品种各 3 日（51 行）**；**SC 无数据**（源端自 04-30 起不再提供 SC 基差，历史已知）
- 合并写回 `data/raw/fundamental/basis_{SYM}.parquet`（17 品种各 +3 行；写前备份 `artifacts/backup_p204/` 17 份；applied 登记 `artifacts/p20_4_basis_applied.json`；SC 文件不动）
- 验证：**17 品种基差最新 = 2026-08-20**，SC = 2026-04-30（如实标注）
- 日志：`artifacts/p20_4_basis_update.log`

### 7.2 D1 完整性检查修复（scripts/p16_daily_signal.py）
- `check_data_integrity` 基差/K 线新鲜度改为**严格口径**：`latest < target_date` 即 WARN（原 STALE_DAYS=5 容差会掩盖滞后——如基差止 08-17 而目标日 08-20 曾静默）
- 修复后 08-20 运行：基差 WARN 仅剩 sc0（112 天，源端缺失），其余品种不再被容差掩盖

### 7.3 空计划日语义标注（D4-附）
- `_to_plan_json` 新增顶层 `plan_note` 字段（`empty_plan_note()`），区分三类空计划：
  1. **数据不完整**：基差最新日早于目标日（sc0 除外）→ "数据不完整：引擎B基差止于 … 非真实空仓判断"
  2. **真实无信号**：基差完整但两引擎均无选中 → "真实无信号：… 空仓"
  3. **结构性 0 手**：有选中但名义 floor 后 0 手 → "真实无信号（结构性0手）"
- 单元测试 4 用例全部通过（数据不完整 / 真实无信号 / 结构性0手 / 非空计划返回 ''）

### 7.4 重跑验证（p16 --date 2026-08-20）
基差补齐 + D1 修复后，**08-20 计划由空转非空**：
| 引擎 | 结果 |
|---|---|
| 引擎 A | 无信号（fold 截断，v8 cache 止 06-29，P6-3b 已知） |
| 引擎 B | 选中 4 品种：al0 (br_rank 0.8016)、cu0 (0.7698)、ta0 (1.0000)、zn0 (0.7183) |
| 组合 | **al0 2手 / ta0 3手 / zn0 1手（共 6 手）**，名义 500,691 CNY（50.1% 权益）|
| 风控 | 黑色系 0%（PASS ≤50%）；总名义 50.1%（PASS ≤100%）|
| 备注 | cu0 选中但加权名义 210k floor 后 0 手（px 157,872×mult 5 = 789k/手）不入计划 |
- 落盘：`artifacts/trade_plans/2026-08-20_sentinel2_plan.json/.csv`（已更新为非空）
- 模拟盘：p17 重跑确认通过 p16 修改后的代码路径，**08-17 状态不变（期末 1,150,947.65 / +15.09%）**；BT 纯 B OOS 期末 1,124,249（505 日，较前值 1,120,279 变化 = 基差补至 08-20 后 BT 覆盖 08-18~20 三个交易日）；08-20 计划为下一执行（T+1 08-21 开盘），超出 p17 回放窗口（OOS_END 硬编码 08-17）→ 无增量入账（如实说明）

### 7.5 P20-4 判定
**IS_PASS: YES**（基差补齐 + D1 修复 + 空计划标注 + 重跑验证全部完成）
