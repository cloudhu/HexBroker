# QA P25 Fresh-Eyes 独立复核报告（严过关 / Yan）

- **日期**：2026-08-22
- **复核对象**：P25 交付 —— p12 影子基线 v2→v8 + v13 自然跨边界缓存重建 + 引擎 A 信号恢复评估
- **模式**：降级模式（团队工具不可用），独立复核，未调用任何 P25 脚本函数，全部独立复算
- **独立复算脚本/日志**：
  - `deliverables/software-hexfutures-ai/qa_p25_independent_verify.py`（自写）
  - `deliverables/software-hexfutures-ai/qa_p25_independent_verify.log`（真实输出）
- **口径铁律核对**：复利口径（compute_metrics）、OOS 2024-07-18 后、生产 cap 口径（base.yaml group_cap=0.5 + group_map、B win252/thr0.7/notional 0.30、组合 A30/B70 vol_target=False、CONTRACTS18）、嵌套零泄漏（fold 纯算术推演 + splitter 不变量）、同窗口对比

---

## 1. 静态审查

### 1.1 p12_s4_shadow_monitor.py 基线切换实现（P25-1）

- `git diff HEAD -- scripts/p12_s4_shadow_monitor.py`（+176/-101）核对：
  - 默认基线 v2→**v8**（`DEFAULT_BASELINE_PATH=V8_PATH`）、候选 rt30 不变、默认快照 `p12_s4_shadow_baseline_v8.csv`；
  - 向后兼容：`--baseline-a/--baseline-b`（路径）、`--baseline-label-a/--baseline-label-b`（标签）、`--baseline-csv`（快照路径）、环境变量 `P12_BASELINE_A/B` 兜底（CLI 优先）；`build_baseline/run_monitor` 参数化透传，默认参数 None→模块常量（保留 D1 修复）；
  - 旧 v2 快照 `p12_s4_shadow_baseline.csv` 保留未动（mtime 08-20 17:09，P12 年代）。
- **测试 7/7 独立复跑**：`pytest tests/test_p12_s4_shadow_monitor.py` → **7 passed in 0.78s**。
- **CLI 实测**：
  - v2 向后兼容模式（`--baseline-a ...v2.parquet --baseline-label-a v2 --baseline-csv /tmp/...`）→ 输出 v2 7952 行/976 日、池化相关 0.6037（n_pairs=453）、对齐日 280 —— 与旧基线完全一致 ✅；
  - v8 默认 monitor：池化 0.3108，`--corr-alert 0.25` → **DRIFT_OK**；默认 0.50 → **DRIFT_ALERT**（基线自身误报）—— 阈值重标定必要性实证成立。
  - 唯一小瑕疵（不影响功能）：v2 兼容模式打印行仍标注"（P25-1：v8 生产基线）"，为文案残留。

### 1.2 v13 重建脚本（p25_cache_rebuild.py）

- 口径与 v8 一致：`group_modeling_v2.py --label-mode cross_z --label-pool all`、cal_return_all=False、cal_split=0.5 内建、walk_forward base.yaml 默认（train 250/test 60/purge 5/embargo 2/rolling）；输出 `signals_cache18_grouped_v13.parquet`（**不覆盖 v8**，v8 mtime 仍为 08-20 13:14 ✅）。
- checkpoint+resume：8 组独立落盘 `artifacts/p25_checkpoints/{group}.parquet`（mtime 09:55→10:31 逐组渐进，与 39m37s 声称一致），resume 跳过已有组、`--force` 重跑、`--skip-rebuild` 仅合并；组行数硬校验对照 v8（precious 960/ferrous_steel 960/ferrous_raw 1440/industrial 1920/agri_oil 960/agri_protein 480/agri_soft 960/chem_energy 944 → 合计 8624）。

### 1.3 前置探针脚本（p25_0_fold_probe.py）

- 方法合理：不训练模型，用真实特征索引（`build_features` + `WalkForwardSplitter`）逐品种统计折数/末折测试窗；`data.end=2026-08-21` 与当前数据一致。输出 `p25_0_fold_probe.csv`（8 组 ×18 品种，逐品种重复冗余但不影响结论）。

### 1.4 git diff 范围

- `hexbroker/` **0 diff** ✅；v8 parquet 未动（mtime 08-20 13:14）✅；修改仅 p12 脚本 + trade_plans（08-21 计划基差刷新重出，P25-3）+ 公众号发布日志（08-19~08-21 常规发布，P25-3 相邻）；新增 = P25 7 个脚本 + 报告 + 独立复核文件 + P24 遗留未跟踪脚本（p24_1/p24_rt30，P24 归档时遗留，非 P25 引入）。

---

## 2. 独立复跑验证（真实输出见 qa_p25_independent_verify.log）

### 2.1 前置探针独立验证（重点）—— 全部 PASS

纯算术推演（不训练）：rolling 模式第 i 折测试窗 = `[i·60+257, i·60+317)`，需 `n ≥ i·60+317` 才生成。

| n_feat | 独立推算折数 | 末折测试窗 | 下一折 test_end | 还需交易日 | 判定 |
|---|---|---|---|---|---|
| 2093 | 30 | [1997, 2057) | **2117** | **24** | ✅ |
| 2097（au0/ag0） | 30 | [1997, 2057) | 2117 | **20** | ✅ |
| 2096（m0） | 30 | [1997, 2057) | 2117 | **21** | ✅ |
| 2039（sc0） | 29 | [1937, 1997) | 2057 | **18** | ✅ |

- 与 `p25_0_fold_probe.csv` 唯一品种交叉核对：18 个品种，n 分布 `{2093: 14 品种, 2096: m0, 2097: au0/ag0, 2039: sc0}`；
- K 线实测：06-30→08-21 = **36** 个交易日、06-29→08-17 含 06-29 = **33** 天尾部（工程师"36/33"口径成立，仅 06-29 是否计入的约定差异）；v8 构建（08-17）后仅新增 4 个交易日（08-18~08-21）；
- **结论确认**：主理人"36 天已跨过边界"**不成立** —— 边界需 n≥2117，当前 2093，自 06-29 起需 **60 个交易日 = 一整折测试窗**（36 已积累 + 24 仍需，36+24=60 ✅）；工程师探针 **正确**。

### 2.2 v13 == v8 逐字节验证 —— 全部 PASS

独立读 parquet（不调 p25_2_byte_compare）：v8/v13 各 **8624 行**、列一致、`(symbol,ts)` index 完全一致；`p_up/exp_ret/is_effective` max_abs_diff **0.000e+00**；全表 `panel.equals` 亦通过（含原始行序）。→ **v13 与 v8 逐字节一致** ✅。

### 2.3 覆盖率独立复算 —— 全部 PASS

| 指标 | v8 | v13 | tail_ext |
|---|---|---|---|
| 行数 | 8624 | 8624 | 9289 |
| 唯一信号日 | 662 | 662 | 698 |
| 末信号日 | 2026-06-29 | 2026-06-29 | 2026-08-21 |
| 07 月信号 | 0 | 0 | 396 |
| 08 月信号 | 0 | 0 | 234 |
| 品种数 | 18 | 18 | 18 |

→ v13 覆盖率与 v8 逐项相等；tail_ext 参考值复现（9289/08-21/234）✅。

### 2.4 三方引擎 A S2(cap0.5) + 引擎 B + A30/B70 OOS 独立复现 —— 全部 PASS

独立搭建（用生产 p3/p5 函数 `engine_a_targets_cs / engine_b_targets / run_engine_row / combo_stats_row`，**不调 p25_3_eval**）：

| cache | 引擎 A S2 OOS Sharpe | 组合 A30/B70 OOS Sharpe | 判定 |
|---|---|---|---|
| v8 | **1.064207** | **0.716549** | ✅ Δ=0 |
| v13 | **1.064207** | **0.716549** | ✅ Δ=0 |
| v8_tail_ext | **1.096311** | **0.725324** | ✅ Δ=0 |
| pureB 参考 | 0.481980 | — | ✅ |

- v13 vs v8 引擎 A S2 OOS Δ=0.000e+00（同构）；做多行 v8/v13=1953、tail_ext=2078；
- 复现值与 P22 登记（引擎 A 侧）完全一致；组合侧比 P22 登记小 Δ≈0.0003 的归因（P23 基差刷新使引擎 B 08-21 新增目标日）成立 —— 本复现用的是当前基差数据，得到 0.716549/0.725324，与 P25 引擎 A 侧数值一致，说明组合侧"同数据同窗"内 v8 vs v13 Δ=0 严格成立 ✅。

### 2.5 P25-1 基线切换独立复算（scipy 直算，不 import p12）—— 全部 PASS

| 指标 | v2-vs-rt30 | v8-vs-rt30 | Δ |
|---|---|---|---|
| 对齐日 corr_n_days | 280 | **235** | **-45** ✅ |
| 池化相关（63 窗） | 0.6037（n=453） | **0.3108**（n=468） | **-0.2929** ✅ |
| 基线信号日 | 976（末 06-11） | **662**（末 06-29） | — |

- v8 信号日 **662** 确认 ✅；v8 末信号日（06-29）晚于 v2（06-11）→ 对齐日减少归因"v8 信号日更稀疏（662<976）"而非"末信号日更早"，**报告归因正确** ✅；
- 阈值 0.50 在 v8 基线下基线自身即报警（0.3108<0.50，实测 DRIFT_ALERT）✅；`--corr-alert 0.25` 校准运行 DRIFT_OK ✅。

### 2.6 S4 v13 vs v8 —— 全部 PASS

池化相关 **1.0000**（n_pairs=900）、corr_n_days=547、fresh_signal=NO（v13 末信号日 06-29 = v8）→ NO_TRIGGER ✅。

### 2.7 P25-3 08-21 pending —— 全部 PASS

- `apply_registry.json`：2026-08-21 state=**pending**、fp=9fe4927e…、note="T+1 无次日 → pending" ✅；
- 计划：引擎 A `has_signal_date=false`、`cache_max=2026-06-29`（fold 截断）；引擎 B ta0 **3 手**（br_rank=0.9921、px=5898.60、mult=10、名义 176,957.86 CNY = **17.7%**）✅；
- `_should_skip_apply`（P24-1）：state=applied+fp 一致→跳过；state=pending+fp 一致→**不跳过（允许重试）** —— 08-24 数据到达后 T+1 成交路径成立；入账只依赖 08-24 K 线、不依赖 08-24 基差 ✅。

---

## 3. 关键裁决

### 3.1 前置探针修正 —— 成立（主理人判断有误，工程师探针正确）

- 边界 test_end=**2117**（第 31 折）、当前 n=2093 → 还需 **24** 交易日（au0/ag0 20、m0 21、sc0 18）—— 独立复算一致；
- "36 天已跨过边界"不成立：36 天 = 06-29 起尾部，v8 构建时（08-17）已含 33 天尾部，仅新增 4 天；真实需求 = 自 06-29 起 **60 交易日 = 一整折测试窗**；
- P21"还需 20-24 交易日"指 **08-21 之后还需**（2117-2093=24），非总量 —— 一致 ✅；
- **v13 重建是否本可避免**：P25-0 探针（~2min）已先行得出"未跨边界"决定性结论，v13 重建（39m37s）在决策意义上**可避免**；但作为"同口径自然重建"的**实证确认**（证明 fold 截断是结构性而非缓存/管线伪影、验证 checkpoint+resume 基建、为 v14 建立同构基线）仍属合理工程，非缺陷。建议后续先探针后重建、或探针通过才重建以省算力。

### 3.2 v13 不升级 —— 成立

- v13 与 v8 同构（Δ=0）、fresh_signal=NO → 不构成升级证据，v13 不替换 v8 正确；
- **tail_ext 作为未来 v14 近似**：tail_ext（末折评估窗延长至 08-21）与 v8 共享窗完全一致、OOS 1.096/0.725 高于 v8 1.064/0.717，方向性证据成立；但 v14 真实跨边界时训练窗右移 60 日，数值不会与 tail_ext 完全相等 —— 报告已标注"末折语义差异、方向一致"，**作为交叉验证合理**；
- **正式恢复时间线**：+20-24 交易日 ≈ 24×7/5≈34 自然日 → **约 2026-09-23~24（09 下旬）** v14 重评 —— 明确且可执行 ✅（注意 sc0 需 18 日、au0/ag0 20、m0 21 会先跨，v14 在 24 日后全 18 品种跨齐）。

### 3.3 影子基线 v2→v8 —— 成立，阈值重标定有合理性与遗留建议

- 对齐日 -45 / 相关 -0.29 为 v8 口径真实特征（cross_z+cal_split=0.5 信号日稀疏、标签体系分化），报告如实披露 ✅；
- **0.25 阈值合理性**：0.25 < 基线自身 0.3108 → 基线不误报（0.50 必误报已实测）；0.25 距基线仅 0.06，作为"显著低于基线才告警"的运营阈值可用；建议后续固化 v8 年代标定值；
- **CLI 校准 vs 改默认权衡**：改默认 0.25 其实**不会破坏 v2 基线自检**（v2 池化 0.6037 > 0.25），仅降低 v2 年代 0.25~0.50 区间的告警灵敏度。当前方案（默认 0.50 + CLI 0.25）在生产默认（v8）下每次裸跑即 DRIFT_ALERT 误报，**存在运营脚枪**。建议二选一：
  1. 默认改为 0.25（并文档化 v2 灵敏度差异），或
  2. 基线感知阈值（`alert = max(0.25, baseline_corr × 0.8)`，读基线快照动态取）。
  不作为 VERIFIED 阻塞项，但建议 v14 前落地。

### 3.4 DISCREPANCY 清单

| # | 级别 | 说明 |
|---|---|---|
| D1 | 轻微（文档） | P25 报告 §2.0 称"**15 品种** n=2093"；独立复算实际 **14 品种** n=2093（al0/cu0/cf0/hc0/i0/j0/jm0/ni0/p0/rb0/sr0/ta0/y0/zn0；m0 为 2096、au0/ag0 为 2097、sc0 为 2039）。不影响边界/还需交易日结论（24 日对多数品种仍成立），但数字应更正为 14。 |
| D2 | 轻微（文档） | "36 天尾部 vs 33 天尾部"计数约定不一致（36 不含 06-29、33 含 06-29）；两数分别对实测 K 线成立，建议统一口径。 |
| D3 | 轻微（文案） | v2 向后兼容模式下 print 仍标注"P25-1：v8 生产基线"（标签行残留），不影响逻辑。 |
| D4 | 建议 | 默认 CORR_ALERT=0.50 在 v8 默认基线裸跑即误报；建议基线感知或默认 0.25（见 3.3）。 |

---

## 4. 最终 QA 裁决

# VERIFIED（含 4 项轻微 DISCREPANCY，均不影响结论）

- **前置探针**：独立复算证实工程师探针正确、主理人"36 天已跨边界"判断错误；fold 边界 = 第 31 折 test_end **2117**、还需 24 交易日（au0/ag0 20、m0 21、sc0 18）—— **成立**；
- **v13 == v8 逐字节一致**（8624 行、max_abs_diff=0）—— **实证确认**；
- **三方对比**：v8/v13 S2 OOS 1.064207、组合 0.716549（Δ=0）；tail_ext 1.096311/0.725324 —— **全部可复现**；
- **P25-1**：对齐日 280→235（-45）、池化相关 0.604→0.311（-0.29）、v8 信号日 662 —— **独立复算一致**；阈值重标定 0.25 合理（0.50 基线自身误报已实测）；
- **P25-3**：08-21 pending（ta0 3 手）状态正确，08-24 自动入账路径明确；
- **升级裁决**：v13 不替换 v8；引擎 A 信号正式恢复待 +20-24 交易日（约 2026-09 下旬）v14 重评 —— **合理**；
- **git 范围**：hexbroker 0 diff、v8 未动 —— **符合**。

**IS_PASS: YES**（P25 工程师实现与结论经独立复核成立；D1-D3 为文档/文案级差异，D4 为后续建议，均不阻塞交付）
