# SENTINEL 数据延长执行报告（2026-08-17，最终）

> **任务**：执行 fetch_tdx_extension.py 方案（MCP 逐合约拉取 + 主力拼接），用 2024-07~2026-08 真新数据做 SENTINEL 最终验证。
> **结果**：**数据延长在此环境不可行**（工具硬限制，已实测确认）——SENTINEL 最终验证以重排分段 OOS（1.2 年）为准。

---

## 一、tdx 逐合约拉取实测（执行记录）

| 合约 | 状态 | 实测 |
|---|---|---|
| AU2612（当前主力，setcode=30, target=1） | ✅ 可拉 | **182 根，2025-11-18~2026-08-17**（解析脚本验证通过） |
| AU2412（2024 下半年主力，已交割） | ❌ 空 | Rows=[]，**已交割合约无历史** |
| AU2508（2025 年主力，已交割） | ❌ 空 | 同上 |

**关键发现**：tdx_kline **仅返回未交割/当前活跃合约**。目标区间（2024-07~2026-08）中 2024-07~2025-11 的主力合约（AU2412/AU2506/AU2512 等）全部已交割 → tdx 无数据。

## 二、缺口不可补的三方确认

| 数据源 | 2024-07~2025-11 段 | 原因 |
|---|---|---|
| sina 本地适配器 | ❌ | 数据同截止 2024-07-17（与基准同源） |
| 通达信 tdx MCP | ❌ | 已交割合约不返回历史 |
| 腾讯自选股 westock | ❌ | 期货仅支持现货（伦敦金/黄金9999） |

**2025-11~2026-08 段虽可拉**（当前主力单合约），但：① 无 120 日 warm-up 历史（特征归一化 normalize_window=120 需要前置数据）；② 与基准序列不连续 → **无法直接跑 SENTINEL**。

## 三、SENTINEL 最终验证状态（维持重排分段）

| 项 | 值 |
|---|---|
| OOS 段 | 2023-05-01 ~ 2024-07-17（1.2 年，240 信号，完全留出） |
| **SENTINEL RL（进化后）** | **年化 +79.6% / 回撤 -6.5% / Sharpe 3.83** |
| 规则基线（top-30%+trend）同段 | 年化 +1.59% / 回撤 -2.6% / Sharpe 0.88 |
| 判定 | RL 决策层显著优于规则基线（同段公平对比） |

**绝对年化 79.6% 仍标注为偏高**（OOS 段行情偏多 + 1.2 年外推），但相对优势（Sharpe 3.83 vs 0.88）稳健。

## 四、主理人裁决

1. **接受环境限制**：真新数据（2024-07 后）验证在当前工具链（sina 同源截止/tdx 仅未交割/westock 仅现货）下**不可执行**——已充分实测（3 源 × 合约级验证）；
2. **SENTINEL 验证结论维持**：四层架构 + 重排 OOS（1.2 年）验证通过，RL 显著优于规则基线；
3. **解锁路径**（未来）：本地 pytdx 网络恢复（公共行情服务器当前不可达）或接入含历史主力连续的数据服务商 → 届时执行真新数据最终验证；
4. **口径纪律**：绝对收益数字（79.6%/3.83）仅作相对比较参考，不作为部署依据。

## 五、交付物

- `scripts/fetch_tdx_extension.py`（脚手架 + 实测结论完整记录）
- `scripts/parse_tdx_kline.py`（tdx 持久化 K 线解析器，已验证可复用）
- 本报告
