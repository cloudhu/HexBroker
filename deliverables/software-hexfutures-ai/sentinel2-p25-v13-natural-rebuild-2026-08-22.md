# Sentinel-2 P25 交付报告：p12 影子基线 v2→v8 + v13 自然跨边界缓存重建 + 引擎 A 信号正式恢复评估

- **日期**：2026-08-22
- **执行人**：software-engineer (Kou / 寇豆码)
- **项目**：HexBroker（E:/Workspace/HexBroker）
- **上游**：P6-P24 全部 QA VERIFIED；P19 生产配置 A30/B70 + nf_b 0.30 已固化；主理人确认各品种 06-29 后已新增 36 个交易日
- **模式**：降级模式（团队工具不可用），独立完成实现并如实报告

---

## 0. 执行摘要（TL;DR）

| 任务 | 结果 | 结论 |
|---|---|---|
| P25-1 影子基线 v2→v8 | ✅ 实现 + 新基线快照 | 基线切换完成（向后兼容）；对齐日 280→235、池化相关性 0.604→0.311，如实说明 |
| P25-2 v13 自然跨边界重建 | ⚠️ **未跨边界**（P25-0 探针实证：fold 网格实际边界 test_end=2117 > n=2093，还需 20-24 个交易日） | **v13 == v8 逐字节一致**（预期内，实证确认） |
| P25-2 引擎 A/组合三方对比 | ✅ v8/v13/tail_ext 重估 | v13 Δ=0（同构）；tail_ext 1.096/0.726 为 v13 未来跨边界后的良好近似（交叉验证） |
| P25-2 S4 影子监控更新 | ✅ v13 vs v8 | fresh_signal=**NO**（末信号日未延伸）→ 不构成升级证据 |
| P25-3 08-21 pending 计划 | ✅ 确认 | ta0 3 手 pending（08-24 未到）；08-24 数据到达后 p23_daily_run --date 2026-08-21 自动入账路径明确 |
| **最终裁决** | — | **v13 不升级为生产缓存**；引擎 A 信号恢复待数据跨过第 31 折边界（~2026-09 下旬） |

---

## 1. P25-1 p12 影子基线 v2→v8（P0，已完成）

### 1.1 实现（scripts/p12_s4_shadow_monitor.py）

- 影子基线从 v2（`signals_cache18_grouped_v2.parquet`）升级为 **v8（`signals_cache18_grouped_v8.parquet`，生产基线）**；候选 rt30 不变。
- 向后兼容：
  - CLI `--baseline-a PATH` / `--baseline-b PATH` 可切换两对比缓存（`--baseline-a .../v2.parquet --baseline-label-a v2` 复现旧 v2-vs-rt30 对比）；
  - 环境变量 `P12_BASELINE_A` / `P12_BASELINE_B` 兜底（CLI 优先）；
  - `--baseline-csv PATH` 指定基线快照路径；默认新文件 `artifacts/p12_s4_shadow_baseline_v8.csv`（标注 v8 基线），旧 `p12_s4_shadow_baseline.csv`（v2 基线）保留不动。
- fresh_signal 判定逻辑不变（比较当前 vs 基线快照中各缓存末信号日）。

### 1.2 新基线快照（artifacts/p12_s4_shadow_baseline_v8.csv）

| 缓存 | 行数 | 信号日 | 覆盖 | 日均品种 | 末信号日 | 滚动IC(63) | 滚动命中率(63) |
|---|---|---|---|---|---|---|---|
| v8（基线） | 8,624 | 662 | 2019-04-01~06-29 | 13.03 | 2026-06-29 | -0.1990 | 0.416 |
| rt30（候选） | 6,066 | 837 | 2019-03-04~07-27 | 7.25 | 2026-07-27 | -0.0257 | 0.528 |
| 共享 | — | — | — | — | — | 池化相关性 0.3108 (n_pairs=468) | — |

### 1.3 基线切换影响（如实说明，artifacts/p25_1_baseline_switch_impact.csv）

| 指标 | v2-vs-rt30（旧） | v8-vs-rt30（新） | Δ |
|---|---|---|---|
| 对齐日（corr_n_days） | 280 | 235 | **-45** |
| 池化相关性（63窗） | 0.6037 | 0.3108 | **-0.2929** |
| 主触发对齐窗（n_common_dates） | 44 | 38 | -6 |

- **对齐日减少的归因**：任务描述"v8 末信号日更早"并不准确——v8 末信号日（06-29）反而晚于 v2（06-11）；对齐日减少的真实原因 = **v8 信号日更稀疏（662 日 vs v2 976 日）**（v8 采用 cross_z 标签 + cal_split=0.5 只返回 60 日测试窗后半 → 信号日少、但日均品种多 13.03 vs 8.15），与 rt30 的重叠日减少。
- **池化相关性骤降（0.604→0.311）的归因**：v8 用 **cross_z 截面标签**（P8-4），rt30 用绝对标签——信号真实分化（P7 已登记同类现象）。**影响：原漂移告警阈值 CORR_ALERT=0.50（v2 基线年代标定）在 v8 基线下基线自身即低于阈值（0.311<0.50）→ 立即误报 DRIFT_ALERT**。与 P7 阈值 0.8→0.5 重标定同类的阈值重标定需求：本实现按 CLI `--corr-alert 0.25`（校准值）运行（代码默认不改，保证 v2 兼容模式行为不变）；建议后续把 v8 基线年代标定值固化。
- **验证 --monitor 输出正常**：fresh_kline=NO / fresh_signal=NO（无新 K 线/新信号，与基线快照 08-21 一致）；主触发 UPGRADE_TRIGGER_RETRO（IC 连续胜 7/5，回顾性、非 fresh-OOS 证据）→ 逻辑不变、工作正常。

---

## 2. P25-2 v13 自然跨边界缓存重建 + 引擎 A 信号正式恢复评估（核心）

### 2.0 ⚠️ 前置探针（P25-0）：fold 网格实际边界 —— 36 个交易日未跨过边界

**决定性事实（scripts/p25_0_fold_probe.py，不训练模型，用真实特征索引跑 WalkForwardSplitter）**：

| 品种组 | n_feat（当前） | 折数 | 最后一折测试窗 | 末信号日 | 还需交易日（跨第 31 折） |
|---|---|---|---|---|---|
| 15 品种（al0/cu0/rb0/...） | 2,093 | 30 | [1997, 2057) | **2026-06-29** | **24** |
| au0/ag0 | 2,097 | 30 | [1997, 2057) | 2026-06-23 | 20 |
| m0 | 2,096 | 30 | [1997, 2057) | 2026-06-24 | 21 |
| sc0 | 2,039 | 29 | [1937, 1997) | 2026-06-18 | 18 |

**判定：0/18 品种末折覆盖到数据末日（08-21）→ v13 仍 fold 截断。**

fold 网格参数（base.yaml 默认，与 v8 构建一致）：train_len=250 / test_len=60 / purge=5 / embargo=2 / mode=rolling。第 i 折测试窗 = [i·60+257, i·60+317)；第 31 折（i=30）测试窗 = [2057, 2117)，需 n ≥ 2117 才生成。当前 n=2093（15 品种）→ **还需 2117-2093=24 个交易日**（au0/ag0 20、m0 21、sc0 18）。

**主理人"36 天应已跨过边界"的前置确认不成立（如实报告）**：
- 36 个交易日 = 06-29（v8 末信号日）到 08-21 的尾部长度，**v8 构建时（数据已到 08-17）就已存在 33 天尾部**——不是 P21 之后新增的；
- P21 文档"跨过下一折边界还需 20~24 个交易日"指的是**在 08-21 基础上还需**（P25-0 独立复算一致：2117-2093=24），而非"从 06-29 起 20-24 天总量"；
- 跨边界的**真实需求 = 自 06-29 起 60 个交易日 = 一整折测试窗**（36 已积累 + 24 仍需，约 2026-09 下旬）。

### 2.1 v13 重建（完成，checkpoint+resume）

- 口径与 v8 **完全一致**（`group_modeling_v2.py` GROUPS_V2 8 组；`--label-mode cross_z --label-pool all`；cal_return_all=False、cal_split=0.5 内建；walk_forward 网格 base.yaml 默认；`load_best_params()` 冠军 HP）。
- 编排：`scripts/p25_cache_rebuild.py`（每组合 checkpoint 落盘 `artifacts/p25_checkpoints/{group}.parquet` + resume，P13 教训）。
- 合并 → `artifacts/signals_cache18_grouped_v13.parquet`（不覆盖 v8）。
- 日志：`artifacts/p25_cache_rebuild.log`。

### 2.2 覆盖率对比（artifacts/p25_cache_coverage.csv）

| 指标 | v8 | v13 | Δ | v8_tail_ext（参考） |
|---|---|---|---|---|
| 行数 | 8,624 | 8,624 | 0 | 9,289 |
| 唯一信号日 | 662 | 662 | 0 | 698 |
| 日均品种 | 13.03 | 13.03 | 0 | 13.31 |
| 末信号日 | 2026-06-29 | 2026-06-29 | 0 | 2026-08-21 |
| 2026-07 月信号数 | 0 | 0 | 0 | 396 |
| 2026-08 月信号数 | 0 | 0 | 0 | 234 |

### 2.3 字节级对比（artifacts/p25_byte_compare.txt）

**v13 == v8 逐字节一致**（ts/symbol/p_up/exp_ret/is_effective 全同，max_abs_diff=0.00e+00）——与 P25-0 探针预期完全一致：追加尾部数据不改变任何既有信号行的特征/标签（滚动/截面化特征只用 t 及以前信息），且 fold 网格未新增折。

### 2.4 引擎 A/组合三方对比（artifacts/p25_engineA_compare.csv，生产 cap 口径）

口径：显式 `load_config("configs/base.yaml")` + 显式传参；top_k=0.30 / min=3 / cap=0.5 / group_map(base.yaml)；B win252/thr0.70；组合 A30/B70 vol_target=False；完整回测（滑点1tick+费0.005%+保证金12%+CONTRACTS18）；OOS 2024-07-18 后。**已先以 P22 登记值复核管线（v8=1.064/0.717、tail_ext=1.096/0.725 完全一致）。**

| 指标 | v8（基线） | v13 | tail_ext（变体） |
|---|---|---|---|
| 引擎 A S2 OOS Sharpe | 1.064207 | **1.064207（Δ=0.000000）** | 1.096311 |
| 引擎 A S2 OOS 复利 | +11.59% | +11.59% | +11.81% |
| 引擎 A S2 OOS seg1/seg2 | 0.068 / 2.079 | 0.068 / 2.079 | 0.068 / 2.178 |
| 组合 A30/B70 OOS Sharpe | 0.716549 | **0.716549（Δ=0.000000）** | 0.725324 |
| 组合 A30/B70 OOS 复利 | +6.74% | +6.74% | +6.81% |
| 纯 B 参考 OOS Sharpe | 0.481980 | 0.481980 | 0.481980 |

> 引擎 A 侧数值与 P22 登记**完全一致**（v8=1.064207、tail_ext=1.096311）。组合侧比 P22 登记小 Δ≈0.0003（v8 0.716875→0.716549、tail_ext 0.725649→0.725324），**归因 = P23 基差刷新（17 品种 08-20→08-21）使引擎 B 在 08-21 新增目标日**（组合 OOS 窗口多 1 个引擎 B 实际收益日；引擎 A 无 08-21 信号故不受影响）——属数据刷新正常演变，非代码差异；本次对比 v8 vs v13 为**同数据同窗**，Δ=0 严格成立。

**三方交叉验证结论**：v13（自然重建）= v8（同构，Δ=0）→ 引擎 A 未恢复 08 月信号（fold 截断结构性）；tail_ext（末折评估窗延长变体）与 v8 共享窗完全一致（P22 已验 shared_rows_identical=True），其 OOS 1.096/0.726 是 **v13 未来跨过 fold 边界后的良好近似**（tail_ext 用同一训练规则在 06-30~08-21 追加评估，无新增折训练；跨边界后的自然信号训练窗会右移 60 日，两者在"末折"语义上有差异，但方向一致）→ **P22 登记的"引擎 A 信号恢复候选"得到自然口径的间接交叉验证，但 v13 本身尚未跨边界，正式恢复仍需数据积累**。

### 2.5 S4 影子监控更新（artifacts/p25_s4_monitor.log，v13 vs v8）

| 指标 | v8 | v13 | 判定 |
|---|---|---|---|
| 末信号日 | 2026-06-29 | 2026-06-29 | — |
| fresh_kline | — | NO（08-21 = 基线 08-21） | 数据书签 |
| **fresh_signal** | — | **NO（v13 末信号日 = v8）** | **不构成升级证据** |
| 滚动 IC（63 窗） | -0.1990 | -0.1990 | 同 |
| 滚动命中率（63 窗） | 0.416 | 0.416 | 同 |
| 池化信号相关性 | — | **1.0000**（n_pairs=900） | 同构缓存 |
| 漂移 | — | DRIFT_OK | — |
| 主触发 | — | NO_TRIGGER（IC 胜 0/5，命中率胜 0/5） | 不触发 |

### 2.6 升级裁决建议

**v13 不替换 v8 为生产缓存（EngineAConfig.signal_cache 维持 v8）**：

1. ❌ ① v13 覆盖到 08-21（引擎 A 恢复信号）——**不满足**（末信号日仍 06-29，P25-0 探针实证 fold 边界 test_end=2117 > n=2093）；
2. ✅ ② 同窗口 OOS 不劣于 v8——**满足**（Δ=0，同构）；
3. ⚠️ ③ 与 tail_ext 一致性——tail_ext（1.096/0.726）为 v13 未来跨边界后的方向性近似（间接交叉验证 P22 候选），但 v13 本身未跨边界，无法直接与 tail_ext 同窗对比。

**结论：引擎 A 信号正式恢复**（fresh_signal=YES、08 月信号回归）**待数据累计再 +20-24 个交易日（约 2026-09 下旬）后重建 v14 重评**；届时 v14 vs v8 的 OOS 对比可与 tail_ext（1.096/0.726）做自然口径交叉验证。

---

## 3. P25-3 08-21 pending 计划状态（P1，已确认）

- **08-21 计划当前状态 = pending（正确）**：`artifacts/daily_runs/apply_registry.json` 登记 `2026-08-21` → `state=pending`、`applied_at=2026-08-21T20:42:36`、note="T+1 无次日 → pending（计划待次日开盘入账）"。
- **计划内容**（P23 基差刷新后重出，非 P21 空计划）：`trade_plans/2026-08-21_sentinel2_plan.json` → 引擎 A 无信号（fold 截断：目标日 > v8 缓存末信号日 06-29）→ **组合退化为纯引擎 B** → **ta0 3 手**（engine B，br_rank=0.9921，px=5898.60，mult=10，名义 176,957.86 CNY，17.7% 名义比；cu0 选中但 floor 后 0 手）；风控 PASS（ferrous_all=0）。
- **08-24（下周一）数据到达后的自动入账预期路径**：
  1. `python scripts/p23_daily_run.py --date 2026-08-21 --update-data`（基差/K 线刷新 + 重出计划 + 增量入账）；
  2. 计划指纹 fp=9fe4927e… 不变（引擎 A 仍无 08-21 信号 → 组合仍纯 B → 计划内容稳定）；
  3. `_should_skip_apply`（P24-1 QA L2 修复）：state=pending 且 fp 一致 → **不跳过**（"pending 重试"路径）→ 调用 `p17.run_apply_date(apply_date="2026-08-21")`；
  4. T+1（08-24）开盘成交 ta0 3 手（锚点续接 + MTM）→ 08-24 K 线存在 → `pending=False` → registry 更新 `state=applied`；
  5. 幂等：后续重复运行 state=applied 且 fp 一致 → 跳过。
- **注意（如实）**：若 08-24 基差仍未发布（滞后 K 线 1 日常态），引擎 B 出 08-24 计划可能同样受影响，但 08-21 计划的入账只依赖 08-24 **K 线**（T+1 开盘成交价），不依赖 08-24 基差——路径不受阻塞。

---

## 4. 落盘清单

| 文件 | 说明 |
|---|---|
| `artifacts/p25_0_fold_probe.csv` | P25-0 fold 边界探针（18 品种逐品种折数/末折/还需交易日） |
| `artifacts/p12_s4_shadow_baseline_v8.csv` | P25-1 新 v8 基线快照（标注 v8 基线） |
| `artifacts/p12_s4_shadow_baseline.csv` | 旧 v2 基线快照（保留不动） |
| `artifacts/p25_1_baseline_switch_impact.csv` | 基线切换影响（对齐日/相关性/触发对齐窗） |
| `artifacts/p25_checkpoints/*.parquet` | v13 重建 8 组合 checkpoint（resume 用） |
| `artifacts/signals_cache18_grouped_v13.parquet` | v13 重建缓存（=v8 逐字节一致，不覆盖 v8） |
| `artifacts/p25_cache_rebuild.log` | v13 重建运行日志 |
| `artifacts/p25_cache_coverage.csv` | 覆盖率三方对比 v8/v13/v8_tail_ext |
| `artifacts/p25_byte_compare.txt` | v13 vs v8 字节级对比 |
| `artifacts/p25_engineA_compare.csv` | 引擎 A/组合三方对比（生产 cap 口径） |
| `artifacts/p25_s4_monitor.log` | S4 影子监控（v13 vs v8） |
| `scripts/p25_0_fold_probe.py` | fold 边界探针脚本 |
| `scripts/p25_cache_rebuild.py` | v13 重建编排（checkpoint+resume） |
| `scripts/p25_1_impact.py` | 基线切换影响脚本 |
| `scripts/p25_2_byte_compare.py` | 字节级对比脚本 |
| `scripts/p25_3_eval.py` | 覆盖率 + 引擎 A/组合评估 |
| `scripts/p25_4_s4_v13.py` | S4 v13/v8 影子监控 |
| `scripts/p12_s4_shadow_monitor.py` | P25-1 影子基线 v2→v8 修改（向后兼容） |

## 5. 运行输出 + 耗时

| 步骤 | 耗时 |
|---|---|
| P25-0 fold 边界探针（18 品种，真实特征索引） | ~2 min |
| P25-1 基线切换实现 + 快照 + 影响分析 + 测试（7/7 PASS） | ~5 min |
| v13 重建（8 组，checkpoint+resume；precious 960/ferrous_steel 960/ferrous_raw 1440/industrial 1920/agri_oil 960/agri_protein 480/agri_soft 960/chem_energy 944 全与 v8 行数一致） | **39m37s** |
| 后处理：合并 + 字节对比 + 覆盖率 + 引擎 A/组合三方 + S4 监控 | 49s |
| **总计** | **~47 min** |

**IS_PASS: YES**（全部任务完成；v13 未跨 fold 边界、与 v8 逐字节一致、不升级为生产缓存为如实结论；P25-0 探针给出 fold 网格实际边界 = 第 31 折 test_end 2117，需再积累 20-24 个交易日）

---

## 6. 全局一致性检查（cross-file）

| 检查项 | 结果 |
|---|---|
| p12 修改向后兼容 | ✅ `tests/test_p12_s4_shadow_monitor.py` 7/7 PASS；`scripts/p21_4_s4_v12.py`、`scripts/p23_3_tail_ext_compare.py` 导入正常 |
| p12 接口签名 | ✅ `build_baseline/run_monitor` 参数化路径/标签/CSV，默认 v8 基线 + rt30 候选 + `p12_s4_shadow_baseline_v8.csv` |
| 所有 P25 新模块 | ✅ 7 个脚本全部可导入，无循环依赖 |
| 评估管线复现性 | ✅ 先以 P22 登记值复核：v8=1.064207/0.716549、tail_ext=1.096311/0.725324 与 P22 引擎 A 侧完全一致（组合侧 Δ≈0.0003 归因基差刷新，见 §2.4 注释） |
| 覆盖/字节/S4 一致性 | ✅ v13==v8 字节一致 ↔ 覆盖率逐项相等 ↔ 引擎 A/组合 Δ=0 ↔ S4 相关性 1.0000 互相印证 |
| 数据完整性 | ✅ 18 品种 K 线 08-21；基差 17 品种 08-21、SC 04-30（源端缺失，历史已知）；未改 hexbroker 包、未覆盖 v8 |

**IS_PASS: YES（最终）**

---

## 7. QA 复核与主理人终裁（2026-08-22，QA VERIFIED）

QA 独立复核（`qa_p25_fresh_eyes_review-2026-08-22.md`）：**VERIFIED（4 项轻微 DISCREPANCY 均不阻塞）**——前置探针纯算术独立推演确认（第 31 折 test_end=2117、还需 24 交易日、au0/ag0 20/m0 21/sc0 18，36+24=60 一整折）；v13==v8 逐字节（max_abs_diff=0.000e+00）；三方对比全部复现（v13 1.064207/0.716549、tail_ext 1.096311/0.725324）；基线切换独立复算（对齐日 235/池化相关 0.311）。

### 主理人终裁（P25）

1. **P25 验收通过**：v13 不升级为生产缓存（fold 边界=2117 未达）；**影子基线 v2→v8 完成**（向后兼容，--corr-alert 0.25 校准运行）；08-21 pending 计划状态正确（08-24 自动入账路径核验成立）
2. **⚠️ 前置探针修正（记录在案）**：主理人"36 天已跨边界"判断有误（36 天=v8 构建时已有的尾部 + 仅 4 天新增）；工程师探针正确——**fold 边界必须精确推算（第 31 折 test_end=2117），不能用尾部天数直觉**；真实需求=自 06-29 起 60 交易日=一整折测试窗
3. **D1-D3 文档级修正登记**（15 品种→14 品种 n=2093、尾部计数约定、v2 兼容标签残留——不影响结论）；**D4 登记 P26 候选**：CORR_ALERT 默认 0.50 在 v8 基线裸跑误报 → 基线感知阈值或默认 0.25
4. **v14 时间线明确**：引擎 A 信号正式恢复 ≈ **2026-09 下旬**（再 +20-24 交易日后 v14 自然重建重评）；tail_ext（1.096/0.725）为 v14 的方向性交叉验证参考
5. **方法论沉淀**：① fold 边界精确推算（探针）优先于数据天数直觉；② 影子基线切换须同步重标定告警阈值（基线分布变化）；③ v13 重建虽可被探针替代（40min 算力），但作为同口径实证与 v14 同构基线属合理工程

---
*P25 交付：sentinel2-p25-v13-natural-rebuild + QA review；开发者指南 v3.29（§9.34）*
