# P18-2 QA 独立复核：历史回测重估（fresh-eyes）

- 日期：2026-08-21
- QA：严过关（software-qa-engineer-3，独立 fresh-eyes 复核）
- 范围：静态审查 / 独立复跑 / 引擎 A 转正独立验证（重点）/ P9 网格独立验证 / 关键裁决
- 复核脚本：`artifacts/_qa_p182_independent.py`（独立构造，不调用 p18/p5/p9/p11 策略函数）、
  `artifacts/_qa_p182_cache_iso.py`（缓存版本隔离）、`artifacts/_qa_p182_rerun.log`（p18 复跑日志）

---

## 1. 静态审查表

| 审查项 | 结论 |
|---|---|
| p18_reestimate.py 复用方式 | 复用 p5 `engine_a_targets_cs/run_engine_row/combo_stats_row`、p3 `engine_b_targets/OOS_START`、p11 `run_combo` —— 同构测量、仅 broker 记账不同 ✅ |
| 修复后 broker 生效 | `run_engine_row` 内部 `BacktestEngine` → `SimBroker`（HEAD d3ec0a6 已用 `_multiplier(symbol)`）；脚本无 monkeypatch / LegacyBroker ✅ |
| 复利口径 | `compute_metrics` 基于 cumprod 权益曲线 ✅ |
| OOS 切分 | `eq[idx >= "2024-07-18"]`（p3 OOS_START，口径铁律）✅ |
| 完整回测 | 全历史回测后切片（P16/P11/P9/P5 一致）✅ |
| 名义约定 | 200k/标的（NOTIONAL_FRAC=0.20，与 P3/P5/P9/P11 一致）✅；P17 生产路径 180k 属另一口径（报告已注明）|
| "S2 无 cap" 语义 | `load_config()` 默认 `group_cap=None` → "无 cap" 标签准确 ✅（base.yaml 的 cap=0.5 仅在显式传入时生效）|
| shadow_account README 标注 | "修复前基线" 标注正确（1,905,743 / 0.572 / 0.568）✅ |
| P17 报告头 | "修复后重估" 段标注正确 ✅ |
| ⚠️ P5 pre_fix 口径 | pre_fix -0.337 来自旧 P5 artifact（**v2 缓存** + P5-era 数据 + buggy），post_fix +0.990 用 **v8 缓存** + 当前数据 + fixed → **混用缓存版本**（见 §3 核心发现）|

## 2. 独立复跑对比

`python scripts/p18_reestimate.py` 复跑 EXIT=0（2m04s），关键数字**逐位复现**：

| 项 | 期望（p18 记录） | 复跑实测 | 判定 |
|---|---|---|---|
| P16 A10/B90 volN OOS Sharpe | 0.566 | 0.566271 | ✅ |
| P16 A10/B90 volY OOS Sharpe | 0.567 | 0.567082 | ✅ |
| P16 volN OOS 复利 / MaxDD | +5.39% / -4.85% | 同 | ✅ |
| P11 win252/thr0.70 | 0.490 | 0.489726 | ✅ |
| P11 win126/thr0.60（新最优） | 0.641 | 0.640927 | ✅ |
| P9 A0.35/B0.65 | 0.731 | 0.731366 | ✅ |
| P9 A10/B90 | 0.561 | 0.561005 | ✅ |
| P5 引擎A S2 无cap | +0.990 | 0.990400 | ✅ |
| P5 引擎A cap0.5 | +1.049 | 1.049（日志） | ✅ |

`artifacts/p18_reestimation.csv` / `p18_engineB_grid_postfix.csv` / `p18_combo_grid_postfix.csv` 内容与复跑输出一致 ✅。P11 排名 5/12→6/12、thr=0.60 在 3/4 档 win 最优（win504 最优为 0.80，0.5399 vs 0.60 的 0.5321）均已核对 ✅。

## 3. 引擎 A 转正独立验证（重点）

**独立构造**（不调用 p18/p5/p9/p11 函数）：v8 缓存按日截面 rank top30% + min=3 + 名义 200k，自行复刻 `_capped_selection`：
- v8 S2 与工程师 `_p18_ea_check.txt` 逐品种一致（13 品种、逐品种 days 全对）✅
- v2 S2 `n_long_rows=1805` 与旧 P5 artifact 完全一致 → 构造忠实 ✅

**关键数字（独立复现，逐位一致）**：
- v8 + fixed 全历史 → OOS Sharpe **+0.9904** / 复利 +10.87% / MaxDD -4.32%（cap0.5 **+1.0486** / +11.33%）✅

**机制验证（OOS-only 逐品种 PnL，fixed vs buggy 全局×10）**：

| 品种 | mult | fixed PnL | buggy PnL | 失真 | 10/mult |
|---|---|---|---|---|---|
| ni0 | ×1 | 2,688 | 27,743 | ×10.3 | 10 |
| jm0 | ×60 | 12,441 | 1,927 | ÷6.5 | 0.167 |
| ag0 | ×15 | 4,215 | 2,778 | ×0.66 | 0.667 |
| al0/zn0/cf0 | ×5 | 15,098/12,098/10,715 | 30,422/24,344/21,534 | ×2 | 2 |
| ×10 品种 | ×10 | — | — | ×1 | 1 |

机制确认：buggy 记账按 `10/mult` 系统性扭曲逐品种 PnL（OOS-only 口径 fixed Sharpe 1.215 vs buggy 0.766）。

### ⚠️ 核心新发现（DISCREPANCY）——缓存版本混用，转正归因错误

隔离矩阵（当前数据、全历史、OOS 切片；`_qa_p182_cache_iso.py`）：

| 信号缓存 | buggy broker | fixed broker |
|---|---|---|
| **v2**（P5 时代） | **-0.1546** | **-0.4643** |
| **v8**（P8-4 生产） | **+0.6255** | **+0.9904** |

- 旧 P5 artifact -0.337 = **v2 缓存** + buggy + P5-era 数据（oos_n=500）；新 +0.990 = **v8 缓存** + fixed + 当前数据（oos_n=502）。
- **"转正"的符号翻转主因是 P8-4 缓存升级（v2→v8），不是 broker 修复**：v8+buggy 已是 **+0.63（正）**；broker 修复在 v8 上的边际贡献为 +0.63→+0.99（+0.36，显著但非转正）；在 v2 上 broker 修复反而更差（-0.15→-0.46）。
- 报告 §4 "旧全局 ×10 记账系统性扭曲其 PnL → 修复后真实口径转正" 的归因**不成立**，需更正为："当前 v8 生产缓存下引擎 A OOS 已为正（buggy +0.63），broker 修复进一步提升至 +0.99；历史 -0.337 为 v2 缓存时代口径"。
- P16/P11/P9 的 pre/post 对比**无此问题**：P9 旧网格同用 v8 缓存（p9_combo_validation.py 显式 V8_PATH）、P11 引擎 B 不依赖信号缓存 → 均为 broker-only 对比，干净 ✅。

## 4. P9 网格独立验证

- A0.35/B0.65：volN **0.7314** / volY **0.7314**（期望 0.731）✅
- A10/B90：volN **0.5610** / volY **0.5618**（期望 0.561）✅
- 与 `p18_combo_grid_postfix.csv` 完全一致。引擎 A/B 日收益相关 0.586（报告未给，属补充信息）。

## 5. 关键裁决

**裁决 1 — 引擎 A 修复后转正是否真实**：数字真实（+0.9904 独立逐位复现）；**归因错误**（转正主因=缓存升级 v2→v8；broker 修复边际 +0.36）。生产含义不变：当前生产配置（v8+fixed）引擎 A OOS 显著为正，值得恢复可交易评估。P15 "信号层近极限" 中 IC/校准类结论不受 broker 影响仍成立；P15 可交易口径数字应按其实际缓存（v11）以修复后口径重估。

**裁决 2 — 生产决策建议评估**：
- ① thr 0.70→0.60 立项复验：**支持**。修复后 12 组合 thr=0.60 在 win63/126/252 三档最优、win504 次优（差 0.008 噪声级）；最优 win126/thr0.60 0.641。注意 win252 上 0.60 vs 0.70 差距小（0.500 vs 0.490），thr 越低持仓越多（long_ratio 高），需成本/容量复核。
- ② 引擎 A 名义上调：**支持**（v8+fixed +0.99 / cap0.5 +1.05；20k→floor 0 手需先恢复可交易；需保证金/容量确认）。
- ③ A0.35/B0.65 恢复可交易后重验：**支持但有 gap**——P9 网格用无 cap 引擎 A；生产引擎 A 用 cap0.5，A0.35/B0.65 在 cap0.5 下未验证（cap0.5 单引擎 +1.05 已知），建议恢复可交易后以 cap0.5 复验组合网格。

**裁决 3 — 报告作废/更新方式**：恰当（保留修复前记录 + 以重估报告为准 + artifact 标注）。P16/P11/P9 pre/post 为 broker-only 对比，干净；**P5 行需加缓存版本 note 并更正归因**。

**裁决 4 — DISCREPANCY 清单**：
1. **[主要] P5 行缓存版本混用（v2 vs v8）+ "broker 修复导致转正"归因错误**（§3 新发现，需工程师更正报告 §4 与 p18_reestimation.csv P5 行 note）。
2. [次要] 报告 §4 "ni0 亏损被放大 10 倍"：OOS-only 分解中 ni0 为**小盈利**（+2,688→+27,743），非亏损（可能为全历史切片口径差异，建议注明口径或改例）。
3. [次要] "OOS 活跃日 ~160/502" 实测 **141-142/502**（轻微高估，样本比声称更稀疏，强化"样本有限"caveat）。
4. [说明] P16 pre/post 混入 cap 约定（旧 no-cap 1.612 vs 新 cap0.5 0.566；影响 ~0.005，不改变结论）。
5. [说明] 0.568（QA P18 验证：OOS-only/180k/生产路径）vs 0.490（P11 网格：全历史/200k）口径差异已在报告 §2 注明，正确。

## 6. 最终 QA 裁决

**VERIFIED（全部关键数字独立复现）with 1 项实质性 DISCREPANCY（P5 引擎 A 转正归因 / 缓存版本混用）**

- 数字层：P16 0.566、P11 0.641、P9 0.731、P5 +0.990（cap0.5 +1.049）全部独立逐位复现，3 个 artifacts CSV 与复跑一致 → **数字可靠，可作生产依据**。
- 归因层：P5 "broker 修复导致引擎 A 转正"表述**需更正**（转正主因 = P8-4 v8 缓存升级；broker 修复边际 +0.36）；`p18_reestimation.csv` P5 行建议加缓存版本 note。
- 其余（README 标注、P17 报告头、生产决策建议方向）均正确，可按建议推进立项；建议 ③ 补充 cap0.5 下 A0.35/B0.65 组合复验。

---
*复核脚本留档：`artifacts/_qa_p182_independent.py`、`artifacts/_qa_p182_cache_iso.py`、`artifacts/_qa_p182_rerun.log`、`artifacts/_qa_p182_independent2.log`*

---

# 追加：Round 2 回归（工程师更正后复核，2026-08-21）

工程师已按 §6 DISCREPANCY 完成更正，QA 逐项复核并重跑 `scripts/p18_reestimate.py`（EXIT=0，2m24s）：

## 更正落点核对（全部通过 ✅）

| 更正项 | 落点 | QA 复核 |
|---|---|---|
| P5 归因更正 | 报告 §4 重写："转正"主因=缓存 v2→v8；broker 修复 v8 上边际 +0.36；v2 上更差；-0.337 为 v2 缓存时代口径 | ✅ 标题/正文/结论速览均已改 |
| 隔离矩阵入 CSV | p18_reestimation.csv 新增 P5-ISO 4 行（v2+buggy -0.154582 / v2+fixed -0.464335 / v8+buggy 0.625519 / v8+fixed 0.990384） | ✅ 与 QA 独立矩阵一致（±0.0001 舍入级） |
| 隔离矩阵入脚本 | scripts/p18_reestimate.py 内置 `_iso_sharpe`（v2/v8 × buggy/fixed，复用 LegacyBroker）并重跑 | ✅ 重跑输出逐位一致 |
| P5 行 note | "旧值基于 v2 缓存(P5 时代)+buggy+P5-era 数据；新值 v8 缓存+fixed（归因见 §4）" | ✅ |
| P16 cap 口径说明 | P16 6 行 note 补"旧值=P9 网格无 cap 口径(v8+buggy)；新值=cap0.5 生产口径；cap 差异 ~0.005" | ✅ |
| ni0 例更正 | §4 改为 OOS-only 实际值"+2,688→+27,743（×10.3）" | ✅ 与 QA 逐品种分解一致 |
| 活跃日更正 | §8 改为"OOS 活跃日实测 ~141/502" | ✅ 与 QA 实测 141-142/502 一致 |

## Round 2 回归结果

- 隔离矩阵输出：v2+buggy -0.1546 / v2+fixed -0.4643 / v8+buggy +0.6255 / v8+fixed +0.9904 —— 与 QA 独立矩阵**完全一致** ✅
- P16 A10/B90 0.566/0.567、P11 win126/thr0.60 0.641 + win252/thr0.70 0.490、P9 A0.35/B0.65 0.731 + A10/B90 0.561/0.562 —— **全部不变** ✅
- p18_reestimation.csv / p18_engineB_grid_postfix.csv / p18_combo_grid_postfix.csv 重生成内容正确 ✅

## Round 2 最终 QA 裁决：**VERIFIED（无遗留 DISCREPANCY）**

数字全部独立复现、归因已更正、标注已补齐，可定稿交付。
