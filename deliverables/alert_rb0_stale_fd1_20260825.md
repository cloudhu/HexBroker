# 告警研判：rb0 信号陈旧 fd=1（2026-08-25 15:01）

**判定：✅ 预期行为，非故障，无需处置。** 该告警是 P0-3「隔夜过期→降级/禁开」设计契约的正确触发；且 13:30 那笔 rb0 成交经核验是「平掉已有空单」而非「新开仓」，不违反禁开。

---

## 一、证据链（实测）

1. **缓存主源最新 ts 实测 = 2026-08-24**
   - `signals_cache18_grouped_v8_tail_ext.parquet`：rb0/ag0/cu0 分别为 517/521/517 行，全缓存 `max_ts=2026-08-24`。
   - 今天 = 2026-08-25（周二）→ `busday_count(08-24, 08-25) = 1` > 阈值 0 → 触发 P0-3 隔夜过期告警。**数学正确，非数据损坏**。

2. **告警文案与配置一致**
   - 文案「已降级/禁开（技术兜底接手）」来自 `scheduler.py:441`（P0-3 去重告警，每品种每交易日仅一次）。
   - `health_check.log` 明确「信号新鲜度阈值 0 交易日 = 隔夜过期（仅当天信号有效，P0-3）」→ 设计预期。

3. **13:30 rb0「TRADE LONG 1」是平空，不是新开仓（关键澄清）**
   - 成交明细：`is_open:"False"` + 意图行「【今平】平空 1 手 螺纹钢(rb0)」→ 平掉 13:01 重启前已有的 rb0 空单（-1 → 0）。
   - 属「有持仓仅风控」允许动作（P0-3：过期信号下「有持仓仅风控 / 无持仓禁开」），**不违反禁开契约**。

4. **P1/P2 修复在运行进程已生效（强证据）**
   - 13:01 进程重启（日志「模拟盘交易系统已启动」）后加载了修复代码。
   - 上午每 60s 一次的 ag0「成本门禁拦截 exp_ret=-0.3432」刷屏（旧兜底格式）**彻底消失** → 正是 P1-1 把技术兜底 `exp_ret` 置 0 / `is_effective=False` 后成本门禁不再被触发的预期效果。
   - 禁开契约落地：13:30 之后至 14:41，rb0/ag0 零新成交，权益稳定在 95088.89。

## 二、为什么 15:01 报、且会持续

- 13:01 重启加载修复代码；15:00 日盘收盘→新交易日 `asof=08-25`，fd 由 0 跳 1，触发首次（去重后）告警。
- 15:00→20:30 夜盘前窗口内，主源仍停在 08-24，fd 恒 = 1，告警状态持续属预期（隔夜过期固有后果，非接线故障）。

## 三、何时解除

- 今晚 **20:30 夜盘刷新自动化**（已改写为 P步-A/B 真管线，且须在 15:00 后运行）会拉入 08-25 日线、跑 p22 重建 → 主源延长到 08-25 → fd=0，告警消失，模型信号恢复驱动开仓。

## 四、主理人需知

- 当前 rb0/ag0 **均无持仓**（13:30 平空后 rb0=0，ag0 全程 0；c0 为 accumulate 模式），夜盘前零风险敞口；告警期间零新开仓，安全。
- **无需重启 / 无需修复**：系统按 P0-3 设计正确降级，这正是「不拿陈旧信号开仓」的意图。
- 真正治本 = 今晚 20:30 任务把 08-25 日线纳入（已具备该能力）。

## 五、文件证据
- 缓存：`artifacts/signals_cache18_grouped_v8_tail_ext.parquet`（rb0 max_ts=2026-08-24）
- 交易日志：`data/paper/trades.log`（13:01 重启 / 13:30:18 平空 / 13:36–14:41 零成交）
- 告警源码：`hexbroker/paper/scheduler.py:441`；配置 `hexbroker/diagnostics/health_check.py:218-225`
