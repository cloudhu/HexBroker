# p22 tail_ext 信号缓存全量重训报告

**日期**：2026-09-02
**触发**：当日 13:33 修复生产主湖 cu0/ni0 换月窗口 k 伪台阶污染（5 行 × 5 列）后，用修复后的湖重算信号
**执行窗口**：15:11:31 → 15:20:00（位于 15:00–20:50 允许写窗口内）

---

## 一、执行结果

| 项目 | 值 |
|---|---|
| 命令 | `scripts/p22_tail_ext.py --force` |
| 退出码 | **0** |
| 耗时 | **8 分 29 秒** |
| 护栏遵守 | 未加 `--force-in-session`；未以任何方式绕过 P1-2 缓存禁写护栏 |
| 重训范围 | 8 个品种组全量重训（checkpoint 已清空） |
| 各组耗时 | industrial 106s / ferrous_raw 88s / precious 59s / ferrous_steel 52s / agri_oil 49s / chem_energy 49s / agri_soft 46s / agri_protein 25s |

### 缓存现状

| 项目 | 值 |
|---|---|
| 路径 | `artifacts/signals_cache18_grouped_v8_tail_ext.parquet` |
| 行数 | **9403**（v8 8624 + 尾 779） |
| 列 | symbol / ts / p_up / exp_ret / is_effective |
| 品种数 | 18 |
| **最新信号日** | **2026-09-01** |
| 起始日 | 2019-04-01 |
| sha256（重建前 13:16） | `ffe0b95e4f1460cc…8ea4922` |
| sha256（重建后 15:20） | `e3bc979a9154899d…06556e6` |

---

## 二、核心验证

### ✅ cu0 2026-08-24（判定依据，通过）

| 字段 | 重建前（污染） | 重建后（修复） | 判定 |
|---|---|---|---|
| p_up | 0.6666666666666666 | **0.5333333333333333** | ✅ 与预期 0.5333 一致 |
| exp_ret | 0.20934374034264017 | 0.06808127459992996 | 已重算 |
| is_effective | True | **False** | ⚠️ 该日信号由「有效」变「无效」 |

污染值 0.6667 已消除，**重算确认生效**。

### ✅ ag0 / rb0 逐位不变（零扰动，通过）

以「按品种排序后全列内容哈希」比对（不受行序影响）：

| 品种 | 重建前哈希 | 重建后哈希 | 判定 |
|---|---|---|---|
| **ag0** | `5225b3a4f5f1b21d` | `5225b3a4f5f1b21d` | ✅ 逐位不变 |
| **rb0** | `5eb993b1cc2254f2` | `5eb993b1cc2254f2` | ✅ 逐位不变 |

进一步：18 个品种中**仅 cu0 与 ni0 变化**，其余 16 个品种（含 au0/hc0/al0/zn0/i0/j0/jm0/m0/p0/y0/sr0/cf0/ta0/sc0）全部逐位不变 —— **组间隔离预期完全成立**（cu0/ni0 属 industrial 组；ag0/au0 属 precious、rb0/hc0 属 ferrous_steel，零扰动）。

### ⚠️ cu0 2026-08-21 未变化（与任务预期不符，已取证定性为良性）

任务预期该日 p_up「应有相应变化」，实测**逐位不变**（p_up 0.9333333333333333、exp_ret 0.7356279040134359）。

取证链条（逐层排除）：

1. **湖确实修复了 08-21**：修复台账 `p43_cu_ni_k_step_repair` 记录 cu0 2026-08-21 close 159021.988783 → 158682.55（偏差 0.2139%）。
2. **特征行当天就变**：将 08-21 bar 还原为污染值后重建特征，该行 **18 列发生变化**（f_gap Δ0.382、f_ret_1 Δ0.209、f_range_pos_20 Δ0.156 …），首个变化日即 2026-08-21。
3. **模型输入窗口确实变**：ts=08-21 的输入窗口 Δ = 3.821973e-01（DIFFERENT）。
4. **模型并非不用这些列**：最近 lag 位上这些特征合计 **317 次分裂**（f_vol_5 53、f_xr_spx_ratio 61、f_xr_uup_ratio 55 …）。
5. **真因 —— 模型局部不敏感**：同一拟合模型对修复/污染两个输入窗口的原始预测均值**逐位相同**
   （fix = pol = +0.126944794）。即该样本未跨越任何分裂阈值，走同一批叶子。08-27 亦同。

> **结论**：特征与信号为 **1:1 对齐、无 lag**（与 MEMORY.md §5 口径一致）。08-21 不变是
> **树模型局部不敏感**所致，**不是未重算、也不是对齐偏移**，判定为**良性**。
>
> ⚠️ 附带提醒：ni0 2026-08-20 同样「修复了 bar 但当日信号不变」，为同一机理。

### 🔬 隔离 A/B 复现（重建保真性的决定性证据）

对 cu0 单独复算尾信号：

| 输入 | cu0 2026-08-24 p_up | cu0 2026-08-24 exp_ret | 对应 |
|---|---|---|---|
| 修复后湖（run A） | 0.533333 | 0.068081275 | = **新缓存** ✅ |
| 仅还原 08-21 bar 为污染值（run B） | 0.666667 | 0.209391791 | ≈ **旧缓存**（0.6667 / 0.20934）✅ |

（run B 与旧缓存的微小差异来自旧缓存当日 08-21 与 08-24 两根 bar 均为污染态，而 run B 只还原了 08-21，方向完全一致。）

**重建保真性得证。**

---

## 三、副作用检查

- **v8 共享段未受扰动**：日志载明「tail_ext 共享行 8624 = v8 行 8624 | p_up 逐行一致 True | exp_ret 逐行一致 True」，即 ≤2026-06-29 的历史段完全未被本次重建改动。
- **训练数据未受影响**：修复日（2026-08-20/21/24）远晚于末折训练窗上界（约 2026-06-29），模型与 Platt 校准器与重建前一致。
- **引擎指标（研究性变体口径，非本次修复引入）**：引擎 A S2 OOS Sharpe 1.062 → 0.909（Δ −0.153）；组合 A30/B70 OOS Sharpe 0.906 → 0.853（Δ −0.053）。

---

## 四、结论

**重建成功。** 核心判定项（cu0 2026-08-24 p_up = 0.5333）达成；实际开仓品种 ag0 / rb0 零扰动；
缓存 9403 行、最新信号日 2026-09-01。

唯一与预期不符项（cu0 2026-08-21 未变）经五层取证定性为**模型局部不敏感的良性现象**，
不影响本次重建的正确性，**无需回退或重跑**。

### 建议后续（不阻塞本次交付）

1. `cu0 2026-08-24` 的 `is_effective` 由 True 翻转为 False —— 若盘中门禁依赖该字段，请确认 08-24 相关决策按新口径复核。
2. 全部 8 组均带 `[!! NOT-VERIFIED]` 标记、`max_diff` 1.0~2.0（p_up 量级上近乎不相关），
   且 au0/ag0/m0/ni0 为 `nan`（v8 行数匹配失败）。此为**既有现象、非本次引入**，
   建议单独立项排查尾折复现一致性。

---

---

## 附录：重建后收尾验证（15:41–15:47）

### A1 全仓回归

| 项目 | 结果 |
|---|---|
| 命令 | `pytest -v`（`PYTHONPATH=E:/Workspace/HexBroker`） |
| 结果 | **1137 passed in 92.10s，EXIT=0** ✅ |
| 基线 | 1137（一致） |

判定：改数据未弄坏仓库其他部分。

### A2 缓存健康检查（走生产 `SignalEngine` 路径）

| 检查项 | 结果 |
|---|---|
| 行数 / 品种 | 9403 / 18 |
| NaN（全列） | **0** |
| 重复 `(symbol, ts)` | **0** |
| `is_effective=True` | 9090 / 9403 |
| 末信号日 | 2026-09-01 |

### A3 ⚠️ 新鲜度门禁现状：18/18 全部 BLOCK —— 但属常态，非故障

以生产口径实测（2026-09-02 15:44，`_trading_lag` 交易日历 lag，阈值 0）：

- 此刻已过日盘收盘 → **R = 2026-09-02**；缓存末信号日 **2026-09-01** → **lag = 1** → 拦截。
- **重建前后缓存末信号日均为 2026-09-01**（已用 before/after 快照比对确认）→ **非本次重建引入**。
- 恢复路径：当晚 **20:30 刷新**补入 2026-09-02 信号 → 夜盘/次日 lag=0 放行。

> ⚠️ 遗留风险（既有，非本次引入）：若 20:30 拉取+补数拖过 **20:42**，`merge_tail_ext`
> 落盘硬护栏会 exit 2 → 湖已更新但缓存未动 → **次日整日被拦**。

### A4 `NOT-VERIFIED` 定性：诊断标志，不影响缓存内容

实测缓存构成：**tail_ext 9403 = v8 8624 + 尾 779**，且共享 **8624 行 p_up/exp_ret 逐位一致 True**。

即：末折复现的 `fold_recs` **仅用于与 v8 比对校验，不写入缓存**；缓存的 v8 段就是 v8 原行。
另 `max_diff = max(Δp_up, Δexp_ret)`，其 1.x 量级多来自**无界的 exp_ret**（`p_up ∈ [0,1]` 不可能差 >1）。

**结论：`NOT-VERIFIED ≠ 缓存损坏`**，为既有现象，本次无需额外处置。

### A5 📝 记忆校正（已写入 `MEMORY.md`）

项目记忆 §五 记载的「P3-B 自然日差 / 阈值 1」**已废弃**（`configs/paper.yaml` 中
`freshness_threshold_days` 明令不得复用）。现行口径为 **P3-C 交易日历 lag / 阈值 0**：

- `lag = idx(R) − idx(信号日)`，R = 最新**已收盘**交易日；`lag ≤ 0` 放行，`≥ 1` 拦截，
  **`None` → 保守拦截**（返回 `10**9` + WARNING，绝不可回退到上一交易日）。
- 已补入口诀：**「日盘收盘后至 20:30 刷新前，全品种 BLOCK 属常态」**，判断是否异常要看
  「缓存末信号日 vs R」，不要看到 BLOCK 就当事故。

§四 亦补入三条：tail_ext 缓存构成、`NOT-VERIFIED` 定性、**重训前必须先快照旧缓存**。

---

### A6 影响面闭环：cu0 `is_effective` 翻转 → **实际影响为零**

此前列为「待主理人裁决」，现已自行查清，可关闭：

| 证据 | 结论 |
|---|---|
| `data/paper/account.json` 持仓 | 仅 **`{ag0: 0.0, rb0: 1.0}`** → **cu0 从未建仓** |
| `configs/paper.yaml` 配置品种 | 仅 **ag0 / rb0 / c0**（c0 为 accumulate 未激活） |
| `data/paper/trades.log` 全文 | **`cu0` 出现 0 次** |
| 2026-08-24 当日 | **零成交**（仅启动 + 快照） |

叠加本次重建对 **ag0 / rb0 逐位不变** → **对模拟盘实仓零影响**。

### A7 🔴 运行态异常（非本次操作引入，需人工处置）

模拟盘进程**已停止**：

- `data/paper/paper.pid` = **17576**，但该 PID **不存在**；python 进程数 = **0**。
- 末条日志 **2026-09-02 15:01:52**（日盘收盘后即停），**无 shutdown 日志** → 非正常退出。

对比既往连续运行模式：

| 日期 | 末条 | 15:00–21:00 条数 | 21:00 后条数 |
|---|---|---|---|
| 2026-08-31 | 23:01:34 | 17 | 177 |
| 2026-09-01 | 22:59:18 | 47 | 220 |
| **2026-09-02** | **15:01:52** | **1** | **0** ⛔ |

**时间上排除本次操作的影响**：进程 15:01:52 停止，本会话首个命令约 15:10 才发出。

- ⚠️ **影响**：若不重启，**今晚夜盘（21:00）与 20:55 窗口将缺席**。
- ℹ️ 附带：`SignalEngine` **无 reload 方法**，`self._caches` 仅在构造时加载 →
  **新缓存须重启进程才生效**；好在进程已在停态，下次启动即读新档。

**建议动作**：重启模拟盘（可用 `start_paper_trading_watchdog.bat`），并留意
`data/paper/paper.pid` 的陈旧 PID 是否阻碍启动。

---

## 五、可复用取证脚本

均位于 `artifacts/_tmp/`（gitignored）：

| 脚本 | 用途 |
|---|---|
| `p22_cache_snapshot.py` | 缓存前后快照（文件 sha256 + 逐品种内容哈希 + 焦点行明细） |
| `p22_cache_diff.py` | 前后快照逐格 diff |
| `verify_feature_lag.py` | 扰动单根 bar，观察哪些**特征日**变化 |
| `verify_signal_lag.py` | 单品种隔离 A/B 复算尾信号 |
| `verify_window_input.py` | 对比模型**输入窗口**是否逐位相同 |
| `verify_importance.py` | 按 lag 位查看分裂重要性 |
| `verify_raw_means.py` | 同一模型对两个输入的原始预测均值比对 |

**注意**：`bars.df` 索引 level-0 用的是**短名**（`cu0`），不是 `LOCAL_MAP` 的 `SHFE.cu`。
