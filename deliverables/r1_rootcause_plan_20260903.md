# R1 根因 — 立项文档（2026-09-03 主理人裁决）

> 状态：**已立项，待明日工程师（13:27 429 重置后）深挖实施**。性质：根治项（「源变对」），非 P0-B 护栏（「不许覆盖」）的替代——两层互补：护栏管融合侧兜底，R1 管拉取侧源头。
> 关联：P0-B 护栏（`66bd61a`，本次 cu0/ni0 事故产物）只是「检测到分歧→不许覆盖→保留旧值」；R1 要求「把 tqsdk 主连换月 seam 的分歧在源头校正，让拉取值与湖内口径对齐」。

## 1. 根因现状（多轮取证后的事实画像）

- **现象史**：tqsdk 主连换月 seam 处系统性名义价分歧 → 08-31 rb0、09-01 全 18 品种、09-02 rb0 连续 SEAM_BASIS_CONFLICT（rb0 实测 08-31/09-01 偏差 **-1.69%/-1.61%**）。
- **当前已有多层防御**（09-02/09-03 演进，全部成熟，但都是「检测-拒绝-回退」型）：
  1. `refresh_pull_local.py` 接缝 v3 三分判定（`_seam_decision`）：接缝日∈稳定段→OK；∉且无扩展区→`ROLLOVER_LEAD_WINDOW` 软放行（fail-closed：湖必须含接缝日行）；∉且有扩展区→硬拒回退 pandadata。
  2. `_seam_nominal_break`（P-NEW 防再发）：接缝**跨源**名义价连续性守卫（防 cu0/ni0 旧约×k锚 幽灵排放）。
  3. k 锚定换算（对齐过滤 + 接缝校验 + 换月守卫 + 幂等重发 ~1e-10）。
  4. Tier-2 sina 备源通道（主路判定失败品种不自愈死锁，17:09 拉取 rb0/hc0 走 `BACKUP_SINA`）。
  5. `p6_4_fill_gaps.py` P0-B 融合护栏（本次事故产物）：既有日期整行保留 + 仅授权覆盖 OI。
- **缺口定位（R1 的核心）**：检测层已有（`p42_dominant_reconcile.py`：dominant 快照 × 湖内复权因子交叉对账，明令「dominant_id 切换日 ≠ 复权因子切换日」类日期不得作 graft rollover_dates 输入）；校正层机制已有（`hexbroker/data/graft.py:155 graft_adjusted(rollover_spreads=...)`：逐合约价格比的精确价差续接，文档明示**不可用 `raw[R]/raw[R-1]` 近似**——18 品种消融证明该近似是净损害）。**但二者未接成一条「换月日自动精确校正」的生产链路**——当前换月日只有三种结局：软放行（领先窗口）/ 拒绝回退备源 / 护栏保护旧值，**没有「精确校正后继续用 tqsdk 主路」**。

## 2. 修法候选（明日工程师深挖后选定，主理人终裁）

### 候选 A：源头精确校正（治本，推荐方向）
refresh_pull_local 换月 seam 处：用 p42 类对账识别真换月日 → 取新旧主力**逐合约价格比**构造 `rollover_spreads` → `graft_adjusted` 精确续接 → 主路不再系统性失败。
- 规模：中（触及 k 锚定换算核心）；风险：中（18 品种消融需重跑）。
- **前置深挖点（工程师必答）**：
  1. tqsdk 侧能否取到新旧主力**逐合约**同日价格（算精确价比的前提）？
  2. rb0 -1.69% 分歧的归属：tqsdk 主连自身口径 vs 湖内 k 锚陈旧？（决定校正对象）
  3. p42 的 dominant 快照（`p41_dominant_snapshot.py` 产物）是否够格当换月日权威源？是否需接入主路运行期？

### 候选 B：护栏分级联动（轻量，治标升级）
p6_4 融合侧把「护栏拦截保护 N 行」的品种升级为「拦截 + 换月状态留痕 + 通知定向校正」，把今天的静默保留变成显式事件。
- 规模：小；风险：低；价值：补审计可观测性，不解决源头。

### 候选 C：混合（推荐落地形态）
A 为主（源头校正），B 作护栏的观测增强（融合侧记录「护栏拦截 + 换月分歧量级」供对账）。

## 3. 验收标准

- 换月窗口品种主路不再系统性 `SEAM_BASIS_CONFLICT` 全量回退（08-31 rb0 / 09-01 全品种 / 09-02 rb0 三类历史样本重放通过或归因清晰）。
- 主路校正后值与湖内口径在接缝处偏差收敛到重叠容差内（对齐日 ~1e-10 级目标）。
- 不削弱既有守卫：R22 铁律（不得引入新「全停」失效模式）；P-NEW 幽灵排放守卫保持有效。
- 回归全绿（基线 1284 + 新增，junitxml 权威计数，`PYTEST_DEBUG_TEMPROOT` 仓库外）。

## 4. 执行建议

1. 明日工程师（恢复后）先做 **R1 深挖**（候选 A 三个深挖点，只读代码级取证）→ 出方案 → QA fresh-eyes 复核 → 主理人终裁后再动生产代码。
2. 与「raw_close 纳保 + M5 堵口」（规格已定稿 `p0_adj_round2_spec_20260903.md`）并行立项，但 **R1 排在其后**（前者规格完备、后者需深挖）。
3. 实施窗口：日盘非交易时段（避开 P1-2 禁写与模拟盘窗口）。

## 5. 风险登记

- 逐合约价比不可得 → 候选 A 退化为「校正不可行，维持现状 + 增强观测」（候选 B），需如实报告不可静默。
- graft 近似价差是净损害（文档铁律）——任何方案不得退回 `raw[R]/raw[R-1]` 近似。
