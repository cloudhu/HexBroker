# 期货大讲堂 · 公众号发布工作流改造概览

> 日期：2026-08-15　|　工作空间：E:/Workspace/HexBroker
> 改造对象：原「云侠·公众号财经故事」自动化 → 「HexBroker·期货大讲堂（每日一期·100期系统课）」

## TL;DR
- ✅ 公众号发布脚本/配置已从 `BrokerBuddy/wechat-scf` 复制到 `HexBroker/wechat-scf`（自包含）
- ✅ 发布管线在 HexBroker 下验证通过（云函数连通 + transform + wenyan 渲染）
- ✅ 期货大讲堂 100 期系统课程规划已生成（curriculum.json / 课程规划.md / progress.json）
- ✅ 自动化任务已改造为每日一期、按课程表顺序发布，默认 PAUSED 待你启用
- ✅ 品牌封面图已生成（default-cover.jpg）

## 1. 发布脚本与配置迁移
源：`E:/Workspace/BrokerBuddy/wechat-scf/` + `~/.workbuddy/skills/wechat-publisher/scripts/`
目标：`E:/Workspace/HexBroker/wechat-scf/`

| 文件 | 作用 |
|---|---|
| `wechat_cloud_publish.py` | 本地发布客户端（transform→render→SCF→草稿箱） |
| `transform_for_wechat.py` | 发布前置变换（frontmatter/版权/中文化，随迁至 SCRIPT_DIR 同目录） |
| `index.py` | 腾讯云 SCF 云函数代码（已部署，固定出口 IP） |
| `scf_config.json` | 配置文件（含云函数 URL / API Key / 公众号 appid / secret） |
| `DEPLOY.md` | 部署文档 |
| `publish.sh` `setup.sh` `wechat_ip_check.py` `wechat_publish.py` `wechat_publish_wrapper.py` | 发布相关辅助脚本 |

依赖（用户级，无需复制）：`wenyan` CLI（`~/.workbuddy/binaries/node/workspace/...`）、云函数已在运行。

## 2. 管线适配
- 修改 `wechat_cloud_publish.py` 的 `PUBLISH_LOG_PATH`：去掉 BrokerBuddy 专属的「云侠交易系统」子目录，改为 `HexBroker/公众号文章/published_log.json`，去重日志归 HexBroker 所有。
- 品牌封面引用改为 `HexBroker/公众号文章/期货大讲堂/assets/default-cover.jpg`（缺失时脚本自动回退网络图）。

## 3. 100 期系统课程规划
生成器：`scripts/gen_futures_curriculum.py`（可重跑复生成）
产出（目录 `公众号文章/期货大讲堂/`）：
- `curriculum.json` — 机读课程表（lessons 数组，含 issue/module/title/key_points/status）
- `课程规划.md` — 人读审阅版大纲
- `progress.json` — 发布进度（current_issue 从 1 开始，发布成功后 +1）

课程体系（8 大模块、100 期，由浅入深）：
1. 期货入门基础（1–15）
2. 交易机制与规则（16–30）
3. 商品期货专题（31–48）
4. 金融期货专题（49–62）
5. 技术分析与交易方法（63–76）
6. 基本面与套期保值（77–85）
7. 风险、合规与心态（86–93）
8. 实战进阶与案例（94–100）

## 4. 自动化任务改造
- 原 `automation-1783215673236`（云侠·公众号财经故事，PAUSED）已改造为：
  - 名称：**HexBroker·期货大讲堂（每日一期·100期系统课）**
  - 工作目录：`E:/Workspace/HexBroker`
  - 节奏：每日 08:30（`FREQ=DAILY;BYHOUR=8;BYMINUTE=30`）
  - 状态：**PAUSED**（待你审阅后启用）
- 工作流：Step0 去重门禁 → 读取进度/课程表 → 研究核实 → 撰写(2000-3000字) → 字数预检 → 保存 → `wechat_cloud_publish.py --no-obfuscate --tag FUTURES` 发布 → 更新进度
- 去重守卫：`--tag FUTURES` 代码层硬拦截同日重复发布（命中 `sys.exit(43)`，绝不调微信 API）

## 5. 工作流验证（已打通）
- `python wechat_cloud_publish.py --check-ip` → 云函数可达，出口 IP `106.53.77.61`（与公众号白名单一致）✅
- `python wechat_cloud_publish.py -f "01-什么是期货.md" --no-obfuscate --dry-run` → transform + wenyan render 成功，生成 9,110 字符 HTML ✅
- 示例第 1 期已生成（仅本地，未发布）：`公众号文章/期货大讲堂/01-什么是期货.md`

## 6. 品牌封面
- 已生成 `公众号文章/期货大讲堂/assets/default-cover.jpg`（1080×864，深蓝+金色，含「期货大讲堂」标题）。

## 启用方式
1. 审阅 `课程规划.md` 与示例第 1 期。
2. 在自动化列表中将「HexBroker·期货大讲堂」状态改为 ACTIVE（或手动触发一次试运行）。
3. 首次正式发布前，建议手动跑一次真实发布命令验证端到端：
   ```bash
   cd "E:/Workspace/HexBroker/wechat-scf" && python wechat_cloud_publish.py -f "E:/Workspace/HexBroker/公众号文章/期货大讲堂/01-什么是期货.md" --cover "E:/Workspace/HexBroker/公众号文章/期货大讲堂/assets/default-cover.jpg" --no-obfuscate --tag FUTURES
   ```
4. 发布后到公众号后台草稿箱确认，并确认 `progress.json` 的 current_issue 已 +1。

## 文件清单
- `HexBroker/wechat-scf/`（发布脚本与配置）
- `HexBroker/公众号文章/期货大讲堂/curriculum.json`
- `HexBroker/公众号文章/期货大讲堂/课程规划.md`
- `HexBroker/公众号文章/期货大讲堂/progress.json`
- `HexBroker/公众号文章/期货大讲堂/01-什么是期货.md`（示例）
- `HexBroker/公众号文章/期货大讲堂/assets/default-cover.jpg`（品牌封面）
- `HexBroker/scripts/gen_futures_curriculum.py`（课程规划生成器）
- 自动化：`automation-1783215673236`（已改造为期货大讲堂，PAUSED）
