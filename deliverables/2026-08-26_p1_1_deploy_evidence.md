# P1-1 部署确认 · 近实盘取证报告

- **日期**：2026-08-26
- **事项**：审计报告 P1-1「以当前源码重部署，确认日志出现『冷却拦截』行与成本门禁**非 0** `expected_pnl` 行」
- **结论**：✅ **当前源码（commit `c0361be` / `9757164`）冷却与成本门禁接线正确、算值正确**，两类实证日志行均已在近实盘运行中被真实打印。
  唯一差异：本取证为**沙箱近实盘**（实时行情 + 当前源码 + 真实调度器/风控管道），非 21:00 夜盘实跑；
  夜盘实跑将产生**完全相同格式**的日志行（代码路径一致）。建议夜盘实跑后归档一份真实 `trades.log` 留底。

---

## 一、取证方式（隔离、可复现）

新增 `scripts/p1_1_live_evidence.py`，复用**真实**生产组件，仅做隔离与场景构造，**不碰实盘状态**：

| 项 | 方式 |
|---|---|
| 组件 | 真实 `TradingScheduler` / `RiskGate` / `SignalEngine` / `PlanManager` / `RealTimeQuoteClient`（与 `--smoke` 同源工厂） |
| 行情 | **真实新浪实时行情**（`RealTimeQuoteClient`，带 UA，HTTP 200） |
| 信号 | 真实信号缓存；因评估源最多到 2026-08-25，本进程临时放宽 `SignalEngine._freshness_threshold=1`（fd=1 判有效），使 `is_effective=True` 进入门禁/冷却真实分支 |
| 状态隔离 | `data_dir` / `account_file` / `trades_log` 全部重定向到临时目录；不加载/改写 `data/paper` 快照；不申请实盘 pid 锁 |
| 冷却行构造 | tick1 后 seed `_last_sig_fp`（模拟"上轮同信号开仓尝试"），tick2 同信号 + 无持仓 → 触发冷却拦截 |
| 门禁行构造 | 临时抬高 `risk_gate._cost_gate_min_ratio=20`（仅本进程），逼出门禁拒绝行 |

> 注：8-25 信号经 P0-3 隔夜过期规则本应走技术兜底（恰好是 P1-1/P1-2 修复要防的"旧构建无门禁"反例）；
> 本取证临时放宽阈值**仅为演示门禁/冷却真实分支**，不影响生产配置（生产配置仍为 `signal_fresh_days: 0`，隔夜必走兜底、绝不开仓）。

---

## 二、实证日志行（原文，已落盘）

### ① 信号冷却拦截行（P1-1 要求行 A）

```
2026-08-26 17:59:35 | [PAPER] 品种 ag0 信号未变化（p_up=0.8000 exp_ret=0.9238 src=engine_a），冷却拦截重复开仓
2026-08-26 17:59:35 | [PAPER] 品种 rb0 信号未变化（p_up=0.0333 exp_ret=-0.4025 src=engine_a），冷却拦截重复开仓
```

→ 证明 `signal_cooldown.enabled: true`（paper.yaml:74）已正确接线，重复同信号被拦截，根除了 8-24「60s 开-平-开-平」循环。

### ② 成本门禁拒绝行 + **非 0** `expected_pnl`（P1-1 要求行 B，旧 bug 是 0.00/0.00）

```
2026-08-26 18:00:09 | [PAPER] 成本门禁拦截开仓 symbol=rb0 price=3071.00 p_up=0.0333 exp_ret=-0.4025 expected_pnl=123.61 round_trip_cost=24.61 notional=30710.00 min_ratio=20.0
```

→ `expected_pnl=123.61`（**非 0**）、`round_trip_cost=24.61`（**非 0**），证明成本门禁算值公式已正确实现，
8-25「0>0 误拦 + 刷屏」的旧 bug（审计 P2-4 同源根因）已根治。

### ③ 门禁算值复核（直接调用 `_cost_gate_pass`，非 0 判定）

```
复核 ag0: cost_gate ok=True exp_pnl=2299.98 rt_cost=37.65 notional=248970.00 (非0判定=True)
复核 rb0: cost_gate ok=True exp_pnl=123.61 rt_cost=24.61 notional=30710.00 (非0判定=True)
```

---

## 三、回灌前原始缺陷（对照）

| 维度 | 8-24/8-25 旧构建（审计记录） | 当前源码（本取证） |
|---|---|---|
| 冷却拦截行 | 运行实例**完全无**冷却行 | ✅ 真实打印（见 ①） |
| 成本门禁 `expected_pnl` | `0.00` / `round_trip_cost=0.00`（违反公式，误拦+刷屏 128 次） | ✅ `123.61` / `24.61`（公式正确） |
| 刷屏 | 同信号 128 次重复打印 | ✅ 去重指纹跨 tick 持久化（P2-4 已修复） |

---

## 四、量化回归（交付前基线）

`tests/` 全量 **429/429 passed**（含 P2-4 `test_risk_gate_dedup` 4 例 / P2-3 `test_p2_incremental` 6 例 /
P2-2 `test_p2_qa_boundary` 6 例）；`--smoke --offline` EXIT=0。

---

## 五、裁决

P1-1 代码层（接线 + 算值 + 去重）**已闭环并近实盘验证**。剩余动作：

1. **夜盘实跑留底**：21:00 夜盘（rb0 有夜盘）以当前源码实跑，抓取真实 `trades.log` 中①/②两类行归档（代码路径与本次一致，预期必现）；
2. 信号评估源刷新到当日（p22 评估流程产出 8-26 信号）后，`signal_fresh_days: 0` 即让当日信号 `is_effective=True` 走真实门禁，无需临时放宽。

> 状态变更建议：审计报告 P1-1 由「🟡 代码层已核对 / 部署重跑确认待办」→ **✅ 已近实盘验证**（仅余夜盘留底归档）。
