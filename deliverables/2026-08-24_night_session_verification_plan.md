# 今晚夜盘（21:00–23:00）修复版真实盘验证方案 · 2026-08-24

> 主理人：齐活林｜验证窗口：今晚夜盘（rb0 21:00–23:00；ag0 延续至 02:30）
> 背景：白天完成修复链——S1 开仓缓冲+MA 带宽（cb37333）、P0-1 开仓成本门禁、P0-2 信号无变化冷却、R3 滑点纳入成本口径、R3-1 min_tick 兜底补全。均通过复跑与 QA fresh-eyes 复核，但 **未在真实盘环境验证**（QA 遗留 R3 项）。本窗口为修复后首个真实盘验证。

---

## 一、验证目标（可观测指标）

| # | 目标 | 观测证据 | 通过标准 |
|---|---|---|---|
| V1 | 开-平循环消除 | 60s 轮询下同品种不再出现"开→平→开→平"；日志出现 `signal_cooldown` 拦截 | 同方向 60s 内开平 ≤1 次 |
| V2 | 成本门禁生效 | 无持仓开仓时低预期信号（ag0 类）出现 `cost_gate_reject` | 门禁按配置拦截并标注 reason |
| V3 | 统计闭合 | 盘中 `[统计]` 行（约 300s）闭合偏差保持 +0.0000 | 偏差=0 或触发 `[告警]` 可排查 |
| V4 | rb0 隔夜多单管理 | T000329@3038（1 手）进入夜盘，止损 2917.89（ATR×2.5）只上移不下移 | 持仓按纪律管理，无异常 |

## 二、前置 checklist（20:45–21:00 执行）

1. **代码版本**：`git log --oneline -1` 确认 HEAD = `5a0c73a`（含全部修复链）。
2. **信号缓存刷新**：`signals_cache18_grouped_v8_tail_ext.parquet` 当前最新 08-21（陈旧 3 天）——开盘前**强制刷新至 08-24 收盘数据**，杜绝陈旧 p_up 开仓（P0-3）。
3. **PID 锁/进程检查**：`tasklist | findstr python` 无残留实例；`data/paper/paper.pid` 无僵尸锁（启动互斥已生效，第二实例会被拒）。
4. **rb0 持仓预案**：3038 多单 1 手——确认止损挂单 2917.89；**不摊平、不锁仓**；若夜盘高开至止盈区按 v4.0 纪律处理。
5. **启动**：`start_paper_trading.bat`（先健康检查全绿再 Step2 启动）。

## 三、观察清单（21:00–23:00）

- **21:00–21:15 重点**：开盘首小时观察是否仍有开-平循环（对比修复前每 60s 循环的行为）。
- **日志关键行**（tail -f data/paper/trades.log）：
  - `signal_cooldown` → 冷却拦截（P0-2 生效）
  - `cost_gate_reject` → 成本门禁拦截（P0-1 生效）
  - `[统计]` / `[告警]` → 闭合校验与数据质量（随快照约 300s）
  - `[意图]` 中文行 → 交易动作可读化
- **每 30 分钟抽查** trades.log 尾部一次，记录笔数与品种。

## 四、通过标准（23:00 复盘）

| 指标 | 修复前基线（8/24 白天） | 预期（修复后） |
|---|---|---|
| 全天去重成交 | 329 笔 | 显著下降（循环消除） |
| 手续费 | ¥3,425.42 | 随笔数大幅下降 |
| 净亏 | -¥5,129.42 | 大幅收窄（循环损失 100% 归因） |
| 开-平循环 | 164 闭环 | ≤2（信号未变即冷却） |
| 闭合偏差 | +0.0000 | 保持 0 或触发告警可排查 |

## 五、风险与红线

- **信号陈旧**（若刷新失败）：不追高、不重仓（v4.0 纪律）；无有效信号不硬开。
- **异常循环/闭合偏差 ≠0**：立即停止实例并保留日志（P0-2 告警闭环已接入）。
- **止损纪律**：只上移不下移；破位即走，不摊平。
- **ag0 夜盘延续 02:30**：23:00 rb0 收盘后，ag0 若继续运行需在 02:30 前复核。

## 六、事后复盘（23:00 后或次日盘前）

1. `python scripts/analyze_trades.py data/paper/trades.log --until-time ""`（全天去重统计 + 闭合校验）。
2. 对比 8/24 白天基线（笔数/手续费/净亏/循环数），输出验证结论。
3. 若 V1–V4 全过 → 修复链真实盘验证 PASS，可关闭 QA 遗留 R3；若 V1 未过 → 定位冷却为何未生效（版本/配置/指纹逻辑）并修复。

---

> 期货杠杆交易风险高。本方案为内部系统验证计划，不构成投资建议。
