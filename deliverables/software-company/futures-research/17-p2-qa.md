# P2 双治理 + 落地（六方案 P2 项）· QA 独立复核

> fresh-eyes 独立复核（非实现者视角）。链路：PM→Architect→Engineer→**QA**→主理人终裁
> 复核项：全量回归 / 红线审计 / 默认零变更 / 账本字节级一致 / 新测试非假绿 / 裁决

## 1. 全量回归

| 项 | 结果 |
|----|------|
| 全量 pytest（addopts=-q，仅进度点） | **612 绿**（597+15），EXIT=0 |
| 失败 / 错误标记 | **0**（grep FAILED/ERROR/assert = 0） |
| 结论 | 无回归 ✅ |

> 注：pyproject `addopts="-q"` 不打印汇总行，以标记计数替代确认（597 基线 + 15 新增 = 612）。

## 2. 红线审计（无证据不翻转 / 零顶层重依赖）

- [QA1] `grep` 扫描 `hexbroker/governance/` 顶层 `import vnpy_ctp/mlflow/qlib/pyqlib/rqalpha/vnpy` → **零命中** ✅
- `governance/` 仅用标准库（`dataclasses`/`enum`/`json`/`os`/`pathlib`/`datetime`）+ 项目既有 `..utils.logging` + `import yaml`（与 `market/rule.py`/`factor/registry.py` 同款轻量依赖，红线合规）✅
- 未改动任何既有交易/信号/风控/回测逻辑 ✅

## 3. 默认零变更核验（diff 核对）

- [QA2] `git diff HEAD -- hexbroker/paper hexbroker/risk hexbroker/backtest hexbroker/feature hexbroker/market hexbroker/factor hexbroker/ml` → **为空** ✅
- `scripts/paper_trading_main.py` 改动：仅**新增** `_run_governance_selfcheck()` 函数 + PID 锁后一行调用；未触及 `_tick`/组件构造/撮合任何数值路径 ✅（R15 零侵入成立）

## 4. 账本字节级一致（可审计性）

- [QA3] `CalibrationLedger.load` → `to_dict()` 与磁盘 `calibration_ledger.json` 逐方案、逐字段比对 → **完全一致**（current 键集 + 各字段值）✅
- 原子写约定（`.tmp`+`os.replace`）与 `paper/broker.py` snapshot 同款；测试 `test_ledger_no_tmp_leftover` 确认无 `.tmp` 残留 ✅

## 5. 新测试非假绿（抽样）

15 用例覆盖：联锁 4 类 reason（not_pass/deprecated/unknown_scheme/no_calibration）+ fail-safe + 账本 JSON 往返 + 追加 history 留痕 + `.tmp` 无残留 + 寄存器 verify_all 对 PASS 缺校准抛错 + 6 方案 YAML 加载 + 启动自检语义（live={1C,2A}, shadow={3A,3B,1B,2B}）。
- `test_registry_verify_all_blocks_pass_without_calibration`：构造 PASS 无 calib → 断言抛 ValueError（真断言，非 pass-through）✅
- `test_interlock_unknown_scheme_fail_safe`：未知方案 → `unknown_scheme` reason（fail-safe 真实生效）✅
- `test_ledger_append_keeps_history`：二次 append 覆盖 current 且旧版进 history（真状态变更）✅
- 端到端冒烟：1C/2A=live，其余=shadow，verify_all 不抛 ✅

## 6. 双治理 / 落地 验收映射

| PRD | 验收 | QA 结论 |
|-----|------|---------|
| P2-1 联锁 | R1-R4 | ✅ assert_safe_to_activate + resolve_scheme_mode(fail-safe) + 未知方案 shadow |
| P2-2 账本 | R5-R8 | ✅ 结构化字段 + load/save/append + 原子写 + 字节级一致 |
| P2-3 寄存器 | R9-R12 | ✅ 枚举 + load + verify_all（PASS 须 calib） + 6 方案登记 |
| P2-4 接线 | R13-R15 | ✅ 启动自检 WARNING+shadow，零侵入 tick，不崩不阻断 |

## 7. 诚实披露（遗留/边界）

- 1C/2A 精确 WR/n 标 `pending_qa`（**未编造**，status=PASS 已满足联锁前置）；其量化指标归档于对应晋升门禁评估文档。
- 1B/2B 精确 WR/n 同样 `pending_qa`，待 walk-forward QA 重跑补全（status 已 NOT_PASS，联锁天然拦截，不阻塞本批次）。
- 运行时动态降级（Q5）留后续批次，本批次仅静态登记 + 启动自检。
- 六方案**终裁归档文档**为独立批次（非本 P2），待主理人择期。

## 8. QA 裁决

**PASS** ✅
- 双治理（联锁 + 账本）与落地（寄存器+Yaml + 启动自检）全部落地，验收口径满足。
- 全量 612 绿、红线合规、默认零行为变更、账本可审计字节级一致。
- 六方案定案忠实登记（不编造），未知精确指标显式 `pending_qa`。
- 建议主理人终裁后 feat+docs 双提交，并 `git fsck --no-dangling` 校验。
