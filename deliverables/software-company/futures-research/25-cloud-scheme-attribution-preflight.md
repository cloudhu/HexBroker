# 云侠侧 scheme 归因补全 · 立项书（preflight，暂缓实施）

> 关联：`docs/scheme-signals-contract.md` §5（云侠侧对齐路线）
> 日期：2026-08-28 · 结论：**今日暂缓**（前置条件未满足，跨项目生产链路不宜本会话实施）

## 1. Preflight 取证结论

| 取证项 | 结果 |
|---|---|
| P0O1门禁结果.json 生成器 | **盘中自动化临时脚本**（如 `temp/_1330_p0o1_d9.py`，每次盘中会话现场生成），调用引擎权威判定器 `cloudxia.strategy._gate_one_buy`；`scripts/`/`cloudxia/` 无稳定写入端（grep 实证 0 命中） |
| 六方案归因字段 | 门禁结果记录 strategy/regime/veto，**无 scheme(1C/2A/3B) 归因**——方案通道在 gate_channels 内部判定，未透出至日志 schema |
| 实施风险 | ①生成器非稳定代码（改自动化 prompt/流程不可控）；②触碰云侠生产盘中链路（实盘账户 261770400000048774）；③跨项目改动（云侠独立 git/CI/纪律） |

## 2. 前置条件（实施前必须完成）

1. **固化产出流程**：把盘中门禁结果从"自动化临时脚本"固化为云侠仓库内的稳定脚本（入 git、有测试）。
2. **归因点设计**：在固化脚本的候选评估处透出 `scheme` 字段（复算 1C CHASE_TIER / 2A REGIME_FAILOPEN / 3B breakout 通道判定，或 gate_channels 打标透传）——纯只读回放，不改交易决策路径。
3. **影子数据积累**：六方案默认影子运行，信号稀疏；至少积累可结算样本（n≥min_n）再接自动化。

## 3. 路线建议（按风险递增）

| 路线 | 内容 | 风险 |
|---|---|---|
| R1 人工供给 | 主理人/回测会话按契约（scheme-signals-contract.md §2）用 HexBroker 写入器 `--set/--csv` 手动写入 | 零（当前即可用） |
| R2 固化+归因 | 完成前置 1/2 后，云侠侧纯增量归因导出脚本 → HexBroker `--csv` 接入 | 中（需云侠 SOP 全流程） |
| R3 实时管线 | 自动化盘中任务直接写 signals.json（定时） | 高（盘中链路，最后做） |

## 4. 当前状态

- **HexBroker 消费端已就绪**：P2-D RuntimeDegrader + 写入器 CLI + 契约文档（`4a343dd`），R1 人工供给即刻可用。
- **云侠侧暂缓**：待前置条件满足后按云侠 SOP（PM→Arch→Eng→QA）另立批次实施 R2。
- 本立项书即今日批次交付（无代码改动）。
