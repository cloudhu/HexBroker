# HexBroker 早期文件去向核查报告（取证）

- **日期**：2026-08-24
- **触发**：用户请求"清理/提交这批早期文件"（摘要记载工作树曾有 96 modified + 21 untracked，含待删临时脚本 `_fix_e3.py`）。续做时发现工作树已干净，故转为取证核查"这批文件去哪了 / 是否丢失或泄密"。
- **结论（TL;DR）**：✅ **无丢失、无泄密、已全部入库**。工作树当前 535 文件全已跟踪且干净；`git fsck` 0 悬空/0 不可达；敏感信息扫描仅命中参数化/环境变量用法，无明文密钥；早期文件主体由 `2c8a40b` 初始化导入（228 文件），后续改动被 2026-08-23 的正常 feature/fix 提交吸收。

---

## 1. 工作树现状（证据 [0]）

| 指标 | 值 | 含义 |
|---|---|---|
| `git status --porcelain` 条目 | **0** | 无未提交改动 |
| `git stash list` | 空 | 无暂存残留 |
| 被忽略项 | 29（全为 `data/`/`__pycache__`/`.venv`/`logs/` 等合法产物） | 无遗留脚本 |
| 全盘检索 `_fix_e3.py` / `*_fix_*` / `tmp_*` / `scratch*` | **0 命中** | 临时脚本已不存在 |
| `origin/main..HEAD` | 空 | 本地与上游一致 |
| `git ls-files` 计数 | **535** | 全量已跟踪 |

> 此前摘要记载的"96 modified + 21 untracked"在当前工作区已不复存在。

## 2. 敏感信息扫描（证据 [1][2]）

**按文件名匹配**（入库文件中）：仅 `hexbroker/feature/tokenizer.py` 与 `tests/test_tokenizer.py`——特征分词器，非密钥文件。

**内容级密钥/令牌扫描**（跳过二进制，`git grep`）：命中 5 处，全部为**非硬编码**用法：

| 文件 | 行 | 用法 | 判定 |
|---|---|---|---|
| `hexbroker/data/sources/tqsdk_source.py` | 20,22 | `password` 构造参数（默认 `None`） | ✅ 参数化 |
| `hexbroker/live/ctp_skeleton.py` | 29,42 | `password=os.environ["HEXBROKER_CTP_PASSWORD"]` | ✅ 环境变量注入 |
| `wechat-scf/index.py` | 157,343-378 | `app_secret` 为入参变量 | ✅ 运行期传入，非入库明文 |

> 扫描模式覆盖：`password/secret/api_key/access_key/private_key/-----BEGIN/ghp_/github_pat_/AKIA/sk-/JWT`。**未发现**任何明文 Token、PAT、AWS Key、私钥块或 `.env` 实值。

## 3. 丢失检测（证据 [3]）

`git fsck --full --unreachable --dangling` 结果：

| 对象 | 数量 |
|---|---|
| dangling commit | **0** |
| dangling blob | **0** |
| unreachable | **0** |

> 无任何被 git 丢弃/不可达的对象 → **没有文件因误操作丢失**。

## 4. 分支与提交拓扑（证据 [4]）

- 分支：仅 `main`（无游离分支、无未合并早期提交）。
- 标签：无。
- 提交总数：`149`（全部位于 `main`，线性历史）。
- 最早 2 个提交：`a96986f Initial commit` → `2c8a40b init: HexBroker 期货 AI 量化平台`。

## 5. 早期文件入库路径（证据 [5][6]）

按"新增文件数"排名的 Top 提交：

| 新增文件 | 提交 | 日期 | 说明 |
|---|---|---|---|
| **228** | `2c8a40b` | 2026-08-16 | **init: HexBroker 期货 AI 量化平台（LightGBM 冠军模型 v4）** ← 早期文件主体 |
| 57 | `3053a16` | 2026-08-23 | chore(scripts): ruff 安全清理批次（F401/F541/E713） |
| 38 | `b513eb0` | 2026-08-20 | feat: Sentinel-2 生产基线 |
| 26 | `6c1c9ce` | 2026-08-23 | chore: ruff F8 清理残余 |
| 22 | `c80b9da` | 2026-08-23 | feat(paper): 模拟盘交易系统 |
| 19 | `ef149fd` | 2026-08-23 | feat: 定点修复 P2 Batch A |
| 12×3 | `dddc4b2`/`876117a`/`3f207dc` | 08-16~23 | fix/feat 各类定点修复 |

**还原推断**：摘要中"96 modified + 21 untracked"是相对某工作态的增量改动，其主体（项目脚手架 228 文件）早已在 `2c8a40b` 入库；后续在工作树上的修改与新增文件，被 2026-08-23 的 ruff 清理（57+26）、模拟盘（22）、P0/P1/P2 定点修复等提交依次吸收，最终收敛为当前 535 文件的干净树。临时脚本 `_fix_e3.py` 在 E3 修复阶段（`bb0ff3b`/`2b0e885` 等）已被移除，既不在版本库也不在磁盘。

---

## 判定

| 核查项 | 结果 |
|---|---|
| 早期文件是否丢失 | ✅ 否（`git fsck` 0 悬空/0 不可达；工作树干净、535 文件全跟踪） |
| 是否存在泄密 | ✅ 否（无明文密钥/Token/PAT/私钥；敏感字段均参数化或环境变量注入） |
| 是否已全部入库 | ✅ 是（init `2c8a40b` 导入主体 + 2026-08-23 各提交吸收增量） |
| `_fix_e3.py` 状态 | ✅ 已清理（不在版本库、不在磁盘） |

**最终结论**：该批次早期文件已随正常开发提交完整入库，无丢失、无敏感泄露，无需额外清理或提交动作。原"清理/提交"任务实质已完成。

## 交付物

- 本报告：`deliverables/early_files_forensic_20260824.md`

## 限制与附注

- 摘要未留存那 96 个文件的具体清单，故无法逐文件名比对；以上以**聚合证据**（全跟踪、无悬空对象、线性单分支历史）作为最强可得证明。
- 密钥扫描为**静态正则匹配**，不替代密钥轮换等专业安全审计；如发现某次曾临时硬编码后又删除，历史 blob 中可能仍残留——但 `git fsck` 显示当前无不可达对象，且当前树内容扫描干净。
