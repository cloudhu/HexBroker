# 明日工程师工作单（2026-09-04，工程师 429 重置 13:27:15 后执行）

> 主理人（Qi）编排｜用户 2026-09-03 20:49 四项裁决确认（QA 🟡5🟢4 处置闭环）
> 前置状态：变异台已停（可安全与 scripts/ 改动并发）；全量回归基线 **1284**（tests=1284/0fail/0err/skipped=1）

## 执行顺序（不可跳序）

| 序 | 任务 | 主规格 | 提交 |
|---|---|---|---|
| ① | P0-adj raw_close 纳保（8 项规格） | `deliverables/p0_adj_round2_spec_20260903.md`（工作区未跟踪，执行时读） | `fix(p0-adj):` |
| ② | M5 堵口（p6_4_fill_gaps.py L813） | 同上 spec 内嵌 | 并入 ① 或独立小提交 |
| ③ | QA 🟡-4 pytest.skip 化 | 本文件第三节（内嵌规格） | 独立小提交 |
| ④ | R1 换月 seam 根因深挖 | `deliverables/r1_rootcause_plan_20260903.md`（HEAD 5a3aff8 已入库） | 按深挖结论定 |

---

## 一、任务 ①②：P0-adj raw_close 纳保 + M5 堵口

- 规格全文见 `deliverables/p0_adj_round2_spec_20260903.md`（已批准，8 项）。
- 要点：Option B 解耦两常量（`MERGE_GUARD_FALLBACK_COLUMNS` 仅弃守判定 / `MERGE_PROTECTED_COLUMNS = FALLBACK + ("raw_close",)`）；L801 变量拆分；归因文本；独立新用例 `test_stage_parse_e2e_preserves_existing_raw_close`；note 补 raw_close。
- **M5 堵口**（QA 变异唯一存活点）：`scripts/p6_4_fill_gaps.py` L813 `valid = oi_overwrite.notna()` 会把新帧 OI=0.0 静默清零主湖既有 OI（数据源缺失填 0 的脏形态）→ 改 `valid = oi_overwrite.notna() & (oi_overwrite > 0)`，配反变异用例。注意 L825 note 统计复用 L801 变量（待拆 `old_price`/`old_prot_stat`）。
- 铁律：R22（不引入新「全停」失效模式）；G1–G5（改主湖必走：备份 + 原子写 + 逐位校验）；基线 1284 回归纪律（见第五节）。

## 二、任务 ③：QA 🟡-4 契约对质用例 pytest.skip 化

**出处**：`deliverables/qa_review_tier2_backup_20260903.md` 🟡-4（commit 8bc3745，QA 严过关）。

**问题**：用例 `test_e2e_backup_json_matches_pandadata_contract` 中 `if ref.exists():` 分支在 `artifacts/p6_4_pull_20260903/ag0.json` 缺失时**静默空转**（零断言通过 = 假绿），干净 checkout / CI / artifacts 清理后会退化。

**修法**（一行级 P2）：
1. 定位含 `ref.exists()` 静默空转模式的契约对质用例（文件名约 `tests/test_refresh_pull_local*.py`，QA 报告八节改动清单列：`tests/test_refresh_pull_local_backup_failclosed.py` 新增 / `tests/test_refresh_pull_local.py` 去重）。
2. 模式改为 fail-visible：
   ```python
   if not ref.exists():
       pytest.skip(f"缺契约 artifact: {ref}")   # 显式留痕，杜绝无断言通过
   ```
3. 确认文件头 `import pytest`。

**验收**：跳过时 junitxml 显示 skipped>0（非静默通过）；artifact 在场时断言照常执行。

## 三、任务 ④：R1 换月 seam 深挖

- 画像 + 三候选 + 前置深挖 3 点见 `deliverables/r1_rootcause_plan_20260903.md`。
- **2026-09-03 20:49 主理人裁决归并**（QA Tier-2 残留 ①③，team-lead 决议并入 R1）：
  - 残留①：备源续接段 graft 返回 `provisional=True`、编排层不消费、主源恢复后无自动重建 → 对应候选 B/C 的「恢复后重建路径」设计点。
  - 残留③：续接窗口跨换月不做 k 分段（`graft_adjusted` 未传 `rollover_spreads`）→ **正是候选 A 的核心缺口**，深挖时一并覆盖「refresh_pull_local.py → graft_adjusted(rollover_spreads=...)」接线可行性。
  - 铁律守住：近似价差（`raw[R]/raw[R-1]`）为净损害，**禁退回**。
- 残留②（sina/akshare 同源无冗余）= 结构性约束，登记长期观察，**不立项**。
- 残留④（include_today 显式传参）= 已核验闭环：20:00 自动化 prompt 已显式 `--include-today`、08:00 已显式 `--exclude-today`。仅手动执行 refresh_pull_local.py 时注意显式传参，无需代码改动。

## 四、未提交文件注意

- `deliverables/p0_adj_round2_spec_20260903.md`：工作区**未跟踪**（约定：随任务① fix 提交一并入库），勿误删。
- 任务③④的中间结论若产出新文档，按惯例 docs 提交。

## 五、回归与提交纪律（全程）

- ⛔ 全量回归**不带路径参数**；`PYTEST_DEBUG_TEMPROOT=$(mktemp -d)`（basetemp 必须仓库外，`--basetemp=artifacts/_tmp/...` 触发 conftest 生产目录护栏假失败 46 条）。
- 权威计数：`pytest --junitxml=<仓库外路径> > out.txt 2>&1` → 解析 testsuite 属性；基线 **1284**（tests=1284/0fail/0err/skipped=1）。
- 提交：显式 `git add`（永不 -A）；feat/test/docs 分离；收尾 `git fsck --no-dangling`（须无输出）；永不 gc。
- 远端状态判定用 `.git/FETCH_HEAD` 首行 sha（沙箱拦截 refs/remotes loose-ref 写入，勿反复尝试）。
